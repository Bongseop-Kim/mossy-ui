# Docs

`src/*/docs/*.md` 문서(foundation/docs, components/docs 등) 작성을 위한 양식 가이드.

## 최우선 원칙 — AI Agent 가독성

docs는 사람보다 **AI Agent가 읽기 적합함이 최우선**이다. 모든 양식 규칙은 이 원칙에서 나온다.

- **자기완결** — 문서 하나만 읽어도 판단할 수 있게 쓴다. 외부 문서 없이는 이해할 수 없는 서술을 피한다.
- **선언적 규칙** — 배경 설명·장식적 서술 대신 즉시 적용 가능한 규칙과 값을 쓴다.
- **코드와의 일치** — 토큰 값, API 키, import 경로는 실제 소스(`src/foundation/default-token-values.ts`, 테마 빌더 등)와 크로스 체크하여 일치시킨다. 동작하지 않는 예시 코드를 넣지 않는다.
- **구조화 우선** — 산문보다 표·불릿·코드 블록으로 정보를 구조화한다.
- **긍정 지시** — "~하지 않는다"보다 "~한다"로 쓴다. AI Agent에는 금지보다 해야 할 행동을 제시하는 것이 효과적이다.
- **무언급** — 없거나 지원하지 않는 기능은 아예 언급하지 않는 것이 가장 효과적이다. 문서에 존재하는 항목은 모두 사용 가능한 것이어야 한다. 단, 지원하는 기능의 동작 경계(레이어 제약 등)는 명시한다.

## 문서 구조

```markdown
# Title                          ← 영문 명사형 제목

~를 위한 가이드.                   ← 한 줄 명사형 인트로 + 마침표

## 원칙                           ← 명사형 섹션명 (원칙, 톤, 구현 방식 등)

- **키워드** — 설명을 한다체로 쓴다.

## 섹션

| 구분 | 항목 A | 항목 B |
| --- | --- | --- |
```

## 규칙

| 항목 | 규칙 |
| --- | --- |
| 제목 | 영문 명사형 (`# Composition`, `# Loading`) |
| 인트로 | 한 줄 명사형 + 마침표 ("일관된 로딩 UI 선택을 위한 가이드.") |
| 섹션명 | 모든 레벨(`##`, `###`)에서 짧은 명사형으로 쓴다 |
| 어조 | "~한다" 선언형. 긍정문·능동문 우선 |
| 불릿 | `**키워드** — 설명` 패턴 |
| 표 | 비교·매핑·토큰 값은 표로 작성 |
| 토큰 표기 | `$color.bg.brand-solid-pressed` 형식. light/dark 값을 모두 기재 |
| 코드 예시 | TSX. 실제 API와 일치 (테마 키는 camelCase: `theme.color.bg.brandSolidPressed`) |
| 서식 | 본문은 문단·표·불릿·코드 블록으로 구성한다 |
| Do/Don't | 좋은 예·나쁜 예 대비가 필요하면 `Do:` / `Don't:` 패턴 사용 |

## 외부 문서 번안

웹 디자인 시스템(seed-design 등) 문서를 참고할 때는 React Native + Expo UI 기준으로 재작성한다.

- **HTML/CSS 개념 치환** — 태그, media query, hover 등은 RN 대응 개념으로 치환한다. 대응 개념이 없는 항목은 문서에서 제외한다.
- **네이티브 경계 명시** — Expo UI 컴포넌트(SwiftUI / Jetpack Compose)는 내부 파츠가 JS에 존재하지 않는다. 이 제약이 패턴 적용 가능 여부를 바꾸면 반드시 기술한다.
- **토큰 치환** — 참조 문서의 팔레트(carrot 등)를 Mossy 팔레트로 치환하고 실제 값과 대조한다.
