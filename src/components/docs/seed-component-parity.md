# Seed Component Parity

Seed Design 구현 진행 상태를 Seed React 컴포넌트 기준으로 점검하기 위한 체크리스트.

## 기준

- **Seed 기준 경로** — `.ai/refs/seed-design/packages/react/src/components`.
- **mossy 기준 경로** — `src/index.ts`가 named export하는 `src/components/*`.
- **체크리스트 기준** — Seed React 컴포넌트 1개를 기본 1행으로 기록하고, compound namespace는 공개 part별 행으로 분해해 Mossy 대응·현재 props·상태·다음 액션을 관리한다.
- **Props 표기** — Seed는 로컬 참조본의 `*Props` 타입 기준으로 명시 props와 상속 props 타입명을 기록한다.
- **상태 점검** — 컴포넌트 추가·수정 전 `Seed Implementation Checklist`의 상태를 확인하고, 작업 후 Mossy 대응·Mossy props·상태·다음 액션·메모를 갱신한다.
- **Mossy 전용 API** — Seed Design 구현 목표에 포함되지 않는 Mossy 전용 컴포넌트는 `Mossy Only` 섹션에서 별도 관리한다.

## 상태

| 상태 | 의미 |
| --- | --- |
| 완료 | Seed 대응 범위를 Mossy RN/Expo UI API로 구현 완료했다. |
| 부분완료 | Mossy 대응 컴포넌트는 있으나 Seed props·파트·variant 대비 누락 범위가 남아 있다. |
| 미구현 | Seed 컴포넌트를 확인했지만 mossy-ui 공개 API에 대응 컴포넌트가 없다. |
| 제약 | DOM/HTML, Ark web primitive, web portal, web-only CSS layout, Expo UI native slot 제약 때문에 Seed API 형태를 그대로 제공하지 않는다. |
| Mossy only | seed-design에는 없으나 mossy-ui에서만 제공한다. |

## Seed Implementation Checklist

| Seed 컴포넌트 | Seed props 요약 | Mossy 대응 | Mossy props | 상태 | Props별 상태 | 다음 액션 | 메모 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `ActionButton` | `ActionButtonProps`: `color?`, `fontWeight?`; 상속 `ActionButtonVariantProps`, `UsePendingButtonProps`, `PrimitiveProps`, `StyleProps.flexGrow/bleedX/bleedY`, button attrs | - | - | 미구현 | `color?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `fontWeight?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `ActionButtonVariantProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `UsePendingButtonProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `PrimitiveProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `StyleProps.flexGrow/bleedX/bleedY`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `button attrs`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 버튼 계열 토큰 API 설계 필요. |
| `ActionChip` | `ActionChipProps`: 상속 `ActionChipVariantProps`, `PrimitiveProps`, button attrs | - | - | 미구현 | `ActionChipVariantProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `PrimitiveProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `button attrs`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Chip 계열 미구현. |
| `ActionSheet` | `Root`: `lazyMount?`, `unmountOnExit?`; `Backdrop`, `CloseButton`, `Content`, `Description`, `Header`, `Item`, `List`, `Positioner`, `Title`, `Trigger` 상속 Dialog/HTML props | - | - | 제약 | `Backdrop`: 제약 — ActionSheet.Backdrop 행에서 props를 별도 관리한다. / `Positioner`: 제약 — ActionSheet.Positioner 행에서 props를 별도 관리한다. / `Content`: 제약 — ActionSheet.Content 행에서 props를 별도 관리한다. / `Header`: 제약 — ActionSheet.Header 행에서 props를 별도 관리한다. / `Root`: 제약 — ActionSheet.Root 행에서 props를 별도 관리한다. / `Title`: 제약 — ActionSheet.Title 행에서 props를 별도 관리한다. / `Description`: 제약 — ActionSheet.Description 행에서 props를 별도 관리한다. / `Trigger`: 제약 — ActionSheet.Trigger 행에서 props를 별도 관리한다. / `List`: 제약 — ActionSheet.List 행에서 props를 별도 관리한다. / `Item`: 제약 — ActionSheet.Item 행에서 props를 별도 관리한다. / `CloseButton`: 제약 — ActionSheet.CloseButton 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `ActionSheet.Backdrop` | `ActionSheetBackdropProps`: 상속 `DialogPrimitive.BackdropProps` | - | - | 제약 | `BackdropProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ActionSheet.Positioner` | `ActionSheetPositionerProps`: 상속 `DialogPrimitive.PositionerProps` | - | - | 제약 | `PositionerProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ActionSheet.Content` | `ActionSheetContentProps`: 상속 `DialogPrimitive.ContentProps` | - | - | 제약 | `ContentProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ActionSheet.Header` | `ActionSheetHeaderProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 제약 | `HeaderProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ActionSheet.Root` | `ActionSheetRootProps`: `lazyMount?`, `unmountOnExit?`; 상속 `ActionSheetVariantProps`, `DialogPrimitive.RootProps` | - | - | 제약 | `RootProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ActionSheet.Title` | `ActionSheetTitleProps`: 상속 `DialogPrimitive.TitleProps` | - | - | 제약 | `TitleProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ActionSheet.Description` | `ActionSheetDescriptionProps`: 상속 `DialogPrimitive.DescriptionProps` | - | - | 제약 | `DescriptionProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ActionSheet.Trigger` | `ActionSheetTriggerProps`: 상속 `DialogPrimitive.TriggerProps` | - | - | 제약 | `TriggerProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ActionSheet.List` | `ActionSheetListProps`: 상속 `div attrs` | - | - | 제약 | `ListProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ActionSheet.Item` | `ActionSheetItemProps`: 상속 `PrimitiveProps`, `ActionSheetItemVariantProps`, `button attrs` | - | - | 제약 | `ItemProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ActionSheet.CloseButton` | `ActionSheetCloseButtonProps`: 상속 `DialogPrimitive.CloseButtonProps` | - | - | 제약 | `CloseButtonProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Article` | `ArticleProps`: 상속 `BoxProps` | - | - | 미구현 | `BoxProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 웹 semantic wrapper. RN에서는 별도 의미가 약하다. |
| `AspectRatio` | `AspectRatioProps`: `ratio?`, `position?`, `overflowX?`, `overflowY?`; 상속 `BoxProps` | - | - | 미구현 | `ratio?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `position?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `overflowX?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `overflowY?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `BoxProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | RN `aspectRatio` 기반 구현 가능 여부 확인 필요. |
| `Avatar` | `Root`, `Image`, `Fallback`, `Badge`, `Stack`; 상속 `AvatarVariantProps`, `AvatarStackVariantProps`, Ark image props, `PrimitiveProps`, HTML attrs | - | - | 미구현 | `Badge`: 미구현 — Avatar.Badge 행에서 props를 별도 관리한다. / `Fallback`: 미구현 — Avatar.Fallback 행에서 props를 별도 관리한다. / `Image`: 미구현 — Avatar.Image 행에서 props를 별도 관리한다. / `Root`: 미구현 — Avatar.Root 행에서 props를 별도 관리한다. / `Stack`: 미구현 — Avatar.Stack 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `Avatar.Badge` | `AvatarBadgeProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `BadgeProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Avatar.Fallback` | `AvatarFallbackProps`: 상속 `Image.FallbackProps` | - | - | 미구현 | `FallbackProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Avatar.Image` | `AvatarImageProps`: 상속 `Image.ContentProps` | - | - | 미구현 | `ImageProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Avatar.Root` | `AvatarRootProps`: 상속 `AvatarVariantProps`, `Image.RootProps` | - | - | 미구현 | `RootProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Avatar.Stack` | `AvatarStackProps`: 상속 `AvatarStackVariantProps`, `div attrs` | - | - | 미구현 | `StackProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Badge` | `BadgeProps`: 상속 `BadgeVariantProps`, `PrimitiveProps`, span attrs | - | - | 미구현 | `BadgeVariantProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `PrimitiveProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `span attrs`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 토큰 variant 래퍼 설계 필요. |
| `BottomSheet` | `Root`, `Trigger`, `Positioner`, `Backdrop`, `Content`, `Header`, `Title`, `Description`, `Body`, `Footer`, `CloseButton`; `Body`는 `paddingX`, `height`, `maxHeight`, `minHeight`, `justifyContent`, `alignItems` 상속 | - | - | 제약 | `Backdrop`: 제약 — BottomSheet.Backdrop 행에서 props를 별도 관리한다. / `Body`: 제약 — BottomSheet.Body 행에서 props를 별도 관리한다. / `Content`: 제약 — BottomSheet.Content 행에서 props를 별도 관리한다. / `Description`: 제약 — BottomSheet.Description 행에서 props를 별도 관리한다. / `Footer`: 제약 — BottomSheet.Footer 행에서 props를 별도 관리한다. / `Header`: 제약 — BottomSheet.Header 행에서 props를 별도 관리한다. / `Positioner`: 제약 — BottomSheet.Positioner 행에서 props를 별도 관리한다. / `Root`: 제약 — BottomSheet.Root 행에서 props를 별도 관리한다. / `Title`: 제약 — BottomSheet.Title 행에서 props를 별도 관리한다. / `Trigger`: 제약 — BottomSheet.Trigger 행에서 props를 별도 관리한다. / `CloseButton`: 제약 — BottomSheet.CloseButton 행에서 props를 별도 관리한다. / `Handle`: 제약 — BottomSheet.Handle 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `BottomSheet.Backdrop` | `BottomSheetBackdropProps`: 상속 `Drawer.BackdropProps` | - | - | 제약 | `BackdropProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `BottomSheet.Body` | `BottomSheetBodyProps`: 상속 `PrimitiveProps`, `Pick<StyleProps, "paddingX" \| "height" \| "maxHeight" \| "minHeight" \| "justifyContent" \| "alignItems">`, `div attrs` | - | - | 제약 | `BodyProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `BottomSheet.Content` | `BottomSheetContentProps`: 상속 `Drawer.ContentProps` | - | - | 제약 | `ContentProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `BottomSheet.Description` | `BottomSheetDescriptionProps`: 상속 `Drawer.DescriptionProps` | - | - | 제약 | `DescriptionProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `BottomSheet.Footer` | `BottomSheetFooterProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 제약 | `FooterProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `BottomSheet.Header` | `BottomSheetHeaderProps`: 상속 `Drawer.HeaderProps` | - | - | 제약 | `HeaderProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `BottomSheet.Positioner` | `BottomSheetPositionerProps`: 상속 `Drawer.PositionerProps` | - | - | 제약 | `PositionerProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `BottomSheet.Root` | `BottomSheetRootProps`: 상속 `BottomSheetVariantProps`, `Drawer.RootProps` | - | - | 제약 | `RootProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `BottomSheet.Title` | `BottomSheetTitleProps`: 상속 `Drawer.TitleProps` | - | - | 제약 | `TitleProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `BottomSheet.Trigger` | `BottomSheetTriggerProps`: 상속 `Drawer.TriggerProps` | - | - | 제약 | `TriggerProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `BottomSheet.CloseButton` | `BottomSheetCloseButtonProps`: 상속 `Drawer.CloseButtonProps` | - | - | 제약 | `CloseButtonProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `BottomSheet.Handle` | `BottomSheetHandleProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 제약 | `HandleProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Box` | `BoxProps`: `as?`, `asChild?`; 상속 `StyleProps`, div attrs | `Box` | `Box`: `children?`, `alignment?`, `testID?`, `modifiers?` | 부분완료 | `as?`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `asChild?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `StyleProps`: 부분완료 — Mossy modifier 또는 일부 layout props로만 대응한다. / `div attrs`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `BoxProps`: 부분완료 — Mossy modifier 또는 일부 layout props로만 대응한다. / `children?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. / `alignment?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. / `testID?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. / `modifiers?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 Expo UI modifier 중심으로 `alignment`, `modifiers`만 공개한다. DOM `as`, HTML attrs는 제공하지 않는다. |
| `Callout` | `Root`, `Content`, `Title`, `Description`, `Link`, `CloseButton`; 상속 `CalloutVariantProps`, `DismissibleRootProps`, `PrimitiveProps`, HTML/button attrs | - | - | 미구현 | `CloseButton`: 미구현 — Callout.CloseButton 행에서 props를 별도 관리한다. / `Content`: 미구현 — Callout.Content 행에서 props를 별도 관리한다. / `Description`: 미구현 — Callout.Description 행에서 props를 별도 관리한다. / `Link`: 미구현 — Callout.Link 행에서 props를 별도 관리한다. / `Root`: 미구현 — Callout.Root 행에서 props를 별도 관리한다. / `Title`: 미구현 — Callout.Title 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `Callout.CloseButton` | `CalloutCloseButtonProps`: 상속 `PrimitiveProps`, `button attrs` | - | - | 미구현 | `CloseButtonProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Callout.Content` | `CalloutContentProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `ContentProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Callout.Description` | `CalloutDescriptionProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 미구현 | `DescriptionProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Callout.Link` | `CalloutLinkProps`: 상속 `PrimitiveProps`, `button attrs` | - | - | 미구현 | `LinkProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Callout.Root` | `CalloutRootProps`: 상속 `CalloutVariantProps`, `DismissibleRootProps` | - | - | 미구현 | `RootProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Callout.Title` | `CalloutTitleProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 미구현 | `TitleProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Celsius` | `CelsiusProps`: `value` | - | - | 미구현 | `value`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 도메인 특화 표시 컴포넌트. |
| `Checkbox` | `Root`: `weight?`; `Indicator`: `unchecked?`, `checked`, `indeterminate?`; `Group`, `Control`, `HiddenInput`, `Label`; 상속 `CheckboxVariantProps`, `CheckmarkVariantProps`, Ark checkbox props | `Checkbox` | `Checkbox`: `ComponentProps<typeof ExpoCheckbox>` | 부분완료 | `Control`: 미구현 — Checkbox.Control 행에서 props를 별도 관리한다. / `Group`: 미구현 — Checkbox.Group 행에서 props를 별도 관리한다. / `HiddenInput`: 제약 — Checkbox.HiddenInput 행에서 props를 별도 관리한다. / `Indicator`: 미구현 — Checkbox.Indicator 행에서 props를 별도 관리한다. / `Label`: 미구현 — Checkbox.Label 행에서 props를 별도 관리한다. / `Root`: 부분완료 — Checkbox.Root 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `Checkbox.Control` | `CheckboxControlProps`: 상속 `CheckmarkVariantProps`, `CheckboxPrimitive.ControlProps` | - | - | 미구현 | `ControlProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Checkbox.Group` | `CheckboxGroupProps`: 상속 `CheckboxGroupVariantProps`, `PrimitiveProps`, `div attrs` | - | - | 미구현 | `GroupProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Checkbox.HiddenInput` | `CheckboxHiddenInputProps`: 상속 `CheckboxPrimitive.HiddenInputProps` | - | - | 제약 | `HiddenInputProps`: 제약 — RN/Expo UI 레이어에서 DOM hidden input contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 DOM hidden input contract를 제공하지 않는다. |
| `Checkbox.Indicator` | `CheckboxIndicatorProps`: `unchecked?`, `checked`, `indeterminate?`; 상속 `svg attrs` | - | - | 미구현 | `IndicatorProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Checkbox.Label` | `CheckboxLabelProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 미구현 | `LabelProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Checkbox.Root` | `CheckboxRootProps`: `weight?`; 상속 `Omit<CheckboxVariantProps, "weight">`, `CheckmarkVariantProps`, `CheckboxPrimitive.RootProps` | `Checkbox` | `ComponentProps<typeof ExpoCheckbox>` | 부분완료 | `RootProps`: 부분완료 — universal `ExpoCheckbox` 단일 래퍼로 Root 입력 동작만 대응한다. | Seed part props와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | universal `ExpoCheckbox` 단일 래퍼로 Root 입력 동작만 대응한다. |
| `Chip` | `Root`, `Label`, `PrefixIcon`, `PrefixAvatar`, `SuffixIcon`; 상속 `ChipVariantProps`, `PrimitiveProps`, button/span/div attrs | - | - | 미구현 | `Root`: 미구현 — Chip.Root 행에서 props를 별도 관리한다. / `Label`: 미구현 — Chip.Label 행에서 props를 별도 관리한다. / `PrefixIcon`: 미구현 — Chip.PrefixIcon 행에서 props를 별도 관리한다. / `PrefixAvatar`: 미구현 — Chip.PrefixAvatar 행에서 props를 별도 관리한다. / `SuffixIcon`: 미구현 — Chip.SuffixIcon 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `Chip.Root` | `ChipRootProps`: 상속 `PrimitiveProps`, `ChipVariantProps`, `button attrs` | - | - | 미구현 | `RootProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Chip.Label` | `ChipLabelProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 미구현 | `LabelProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Chip.PrefixIcon` | `ChipPrefixIconProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `PrefixIconProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Chip.PrefixAvatar` | `ChipPrefixAvatarProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `PrefixAvatarProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Chip.SuffixIcon` | `ChipSuffixIconProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `SuffixIconProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `ChipTabs` | `Root`, `List`, `Trigger`, `Content`, `Carousel`, `CarouselCamera`; 상속 `ChipTabsVariantProps`, tabs primitive props | - | - | 미구현 | `Content`: 미구현 — ChipTabs.Content 행에서 props를 별도 관리한다. / `List`: 미구현 — ChipTabs.List 행에서 props를 별도 관리한다. / `Root`: 미구현 — ChipTabs.Root 행에서 props를 별도 관리한다. / `CarouselCamera`: 미구현 — ChipTabs.CarouselCamera 행에서 props를 별도 관리한다. / `Carousel`: 미구현 — ChipTabs.Carousel 행에서 props를 별도 관리한다. / `Trigger`: 미구현 — ChipTabs.Trigger 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `ChipTabs.Content` | `ChipTabsContentProps`: 상속 `TabsPrimitive.ContentProps` | - | - | 미구현 | `ContentProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `ChipTabs.List` | `ChipTabsListProps`: 상속 `TabsPrimitive.ListProps` | - | - | 미구현 | `ListProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `ChipTabs.Root` | `ChipTabsRootProps`: 상속 `ChipTabsVariantProps`, `TabsPrimitive.RootProps` | - | - | 미구현 | `RootProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `ChipTabs.CarouselCamera` | `ChipTabsCarouselCameraProps`: 상속 `TabsPrimitive.CarouselCameraProps` | - | - | 미구현 | `CarouselCameraProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `ChipTabs.Carousel` | `ChipTabsCarouselProps`: 상속 `TabsPrimitive.CarouselProps` | - | - | 미구현 | `CarouselProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `ChipTabs.Trigger` | `ChipTabsTriggerProps`: 상속 `TabsPrimitive.TriggerProps` | - | - | 미구현 | `TriggerProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Columns` | `ColumnsProps`: 상속 `BoxProps`; `ColumnProps`: `width?` | - | - | 미구현 | `BoxProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `ColumnProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `width?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | RN flex/grid 대응 정책 확인 필요. |
| `ConsistentWidth` | `ConsistentWidthProps`: `children`; 상속 `PrimitiveProps`, span attrs | - | - | 제약 | `children`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `PrimitiveProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `span attrs`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | 웹 텍스트 측정/hidden span 기반 유틸 성격이다. |
| `ContentPlaceholder` | `Root`: 상속 `ContentPlaceholderVariantProps`, `PrimitiveProps`, div attrs; `Asset`: HTML attrs | - | - | 미구현 | `Root`: 미구현 — ContentPlaceholder.Root 행에서 props를 별도 관리한다. / `Asset`: 미구현 — ContentPlaceholder.Asset 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `ContentPlaceholder.Root` | `ContentPlaceholderRootProps`: 상속 `ContentPlaceholderVariantProps`, `PrimitiveProps`, `div attrs` | - | - | 미구현 | `RootProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `ContentPlaceholder.Asset` | `ContentPlaceholderAssetProps`: 상속 `HTML attrs` | - | - | 미구현 | `AssetProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `ContextualFloatingButton` | `ContextualFloatingButtonProps`: 상속 `ContextualFloatingButtonVariantProps`, `UsePendingButtonProps`, `PrimitiveProps`, button attrs | - | - | 미구현 | `ContextualFloatingButtonVariantProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `UsePendingButtonProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `PrimitiveProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `button attrs`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | FAB 계열과 함께 검토. |
| `ControlChip` | `ControlChipProps`, `ControlChipBaseProps`: 상속 `ControlChipVariantProps`, `PrimitiveProps`, button attrs | - | - | 미구현 | `ControlChipProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `ControlChipBaseProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `ControlChipVariantProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `PrimitiveProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `button attrs`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 선택형 chip API 필요. |
| `Count` | `CountProps`: 상속 span attrs | - | - | 미구현 | `span attrs`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 숫자 표시 토큰 컴포넌트. |
| `Dialog` | `Root`: `lazyMount?`, `unmountOnExit?`; `Trigger`, `Positioner`, `Backdrop`, `Content`, `Header`, `Title`, `Description`, `Footer`, `Action`; 상속 Dialog primitive/HTML props | - | - | 제약 | `Backdrop`: 제약 — Dialog.Backdrop 행에서 props를 별도 관리한다. / `Positioner`: 제약 — Dialog.Positioner 행에서 props를 별도 관리한다. / `Content`: 제약 — Dialog.Content 행에서 props를 별도 관리한다. / `Description`: 제약 — Dialog.Description 행에서 props를 별도 관리한다. / `Footer`: 제약 — Dialog.Footer 행에서 props를 별도 관리한다. / `Header`: 제약 — Dialog.Header 행에서 props를 별도 관리한다. / `Root`: 제약 — Dialog.Root 행에서 props를 별도 관리한다. / `Title`: 제약 — Dialog.Title 행에서 props를 별도 관리한다. / `Trigger`: 제약 — Dialog.Trigger 행에서 props를 별도 관리한다. / `Action`: 제약 — Dialog.Action 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `Dialog.Backdrop` | `DialogBackdropProps`: 상속 `DialogPrimitive.BackdropProps` | - | - | 제약 | `BackdropProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Dialog.Positioner` | `DialogPositionerProps`: 상속 `DialogPrimitive.PositionerProps` | - | - | 제약 | `PositionerProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Dialog.Content` | `DialogContentProps`: 상속 `DialogPrimitive.ContentProps` | - | - | 제약 | `ContentProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Dialog.Description` | `DialogDescriptionProps`: 상속 `DialogPrimitive.DescriptionProps` | - | - | 제약 | `DescriptionProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Dialog.Footer` | `DialogFooterProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 제약 | `FooterProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Dialog.Header` | `DialogHeaderProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 제약 | `HeaderProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Dialog.Root` | `DialogRootProps`: `lazyMount?`, `unmountOnExit?`; 상속 `DialogVariantProps`, `DialogPrimitive.RootProps` | - | - | 제약 | `RootProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Dialog.Title` | `DialogTitleProps`: 상속 `DialogPrimitive.TitleProps` | - | - | 제약 | `TitleProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Dialog.Trigger` | `DialogTriggerProps`: 상속 `DialogPrimitive.TriggerProps` | - | - | 제약 | `TriggerProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Dialog.Action` | `DialogActionProps`: 상속 `PrimitiveProps`, `button attrs` | - | - | 제약 | `ActionProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Divider` | `DividerProps`: `as?`, `color?`, `thickness?`, `orientation?`, `inset?`; 상속 hr attrs | `Divider` | `Divider`: `orientation?`, `thickness?`, `color?`, `inset?`, `testID?`, `modifiers?` | 부분완료 | `as?`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `color?`: 완료 — Mossy 공개 props에서 동일 이름으로 제공한다. / `thickness?`: 완료 — Mossy 공개 props에서 동일 이름으로 제공한다. / `orientation?`: 완료 — Mossy 공개 props에서 동일 이름으로 제공한다. / `inset?`: 완료 — Mossy 공개 props에서 동일 이름으로 제공한다. / `hr attrs`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `testID?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. / `modifiers?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 `orientation`, `thickness`, `color`, `inset` 토큰/값 중심이다. DOM `as`는 제공하지 않는다. |
| `ExtendedActionSheet` | `Root`: `lazyMount?`, `unmountOnExit?`; `Backdrop`, `CloseButton`, `Content`, `Footer`, `Group`, `Header`, `Item`, `List`, `Positioner`, `Title`, `Trigger`; 상속 Dialog/HTML props | - | - | 제약 | `Backdrop`: 제약 — ExtendedActionSheet.Backdrop 행에서 props를 별도 관리한다. / `Positioner`: 제약 — ExtendedActionSheet.Positioner 행에서 props를 별도 관리한다. / `Content`: 제약 — ExtendedActionSheet.Content 행에서 props를 별도 관리한다. / `Footer`: 제약 — ExtendedActionSheet.Footer 행에서 props를 별도 관리한다. / `Header`: 제약 — ExtendedActionSheet.Header 행에서 props를 별도 관리한다. / `Root`: 제약 — ExtendedActionSheet.Root 행에서 props를 별도 관리한다. / `Title`: 제약 — ExtendedActionSheet.Title 행에서 props를 별도 관리한다. / `Trigger`: 제약 — ExtendedActionSheet.Trigger 행에서 props를 별도 관리한다. / `List`: 제약 — ExtendedActionSheet.List 행에서 props를 별도 관리한다. / `Group`: 제약 — ExtendedActionSheet.Group 행에서 props를 별도 관리한다. / `Item`: 제약 — ExtendedActionSheet.Item 행에서 props를 별도 관리한다. / `CloseButton`: 제약 — ExtendedActionSheet.CloseButton 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `ExtendedActionSheet.Backdrop` | `ExtendedActionSheetBackdropProps`: 상속 `DialogPrimitive.BackdropProps` | - | - | 제약 | `BackdropProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ExtendedActionSheet.Positioner` | `ExtendedActionSheetPositionerProps`: 상속 `DialogPrimitive.PositionerProps` | - | - | 제약 | `PositionerProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ExtendedActionSheet.Content` | `ExtendedActionSheetContentProps`: 상속 `DialogPrimitive.ContentProps` | - | - | 제약 | `ContentProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ExtendedActionSheet.Footer` | `ExtendedActionSheetFooterProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 제약 | `FooterProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ExtendedActionSheet.Header` | `ExtendedActionSheetHeaderProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 제약 | `HeaderProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ExtendedActionSheet.Root` | `ExtendedActionSheetRootProps`: `lazyMount?`, `unmountOnExit?`; 상속 `ExtendedActionSheetVariantProps`, `DialogPrimitive.RootProps` | - | - | 제약 | `RootProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ExtendedActionSheet.Title` | `ExtendedActionSheetTitleProps`: 상속 `DialogPrimitive.TitleProps` | - | - | 제약 | `TitleProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ExtendedActionSheet.Trigger` | `ExtendedActionSheetTriggerProps`: 상속 `DialogPrimitive.TriggerProps` | - | - | 제약 | `TriggerProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ExtendedActionSheet.List` | `ExtendedActionSheetListProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 제약 | `ListProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ExtendedActionSheet.Group` | `ExtendedActionSheetGroupProps`: 상속 `div attrs` | - | - | 제약 | `GroupProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ExtendedActionSheet.Item` | `ExtendedActionSheetItemProps`: 상속 `PrimitiveProps`, `ExtendedActionSheetItemVariantProps`, `button attrs` | - | - | 제약 | `ItemProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ExtendedActionSheet.CloseButton` | `ExtendedActionSheetCloseButtonProps`: 상속 `DialogPrimitive.CloseButtonProps` | - | - | 제약 | `CloseButtonProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `ExtendedFab` | `ExtendedFabProps`: 상속 `ExtendedFabVariantProps`, `PrimitiveProps`, button attrs | - | - | 미구현 | `ExtendedFabVariantProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `PrimitiveProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `button attrs`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Expo UI platform FAB 가능성 확인 필요. |
| `Fab` | `FabProps`: 상속 `FabVariantProps`, `PrimitiveProps`, button attrs | - | - | 미구현 | `FabVariantProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `PrimitiveProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `button attrs`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Expo UI platform FAB 가능성 확인 필요. |
| `Field` | `Root`, `Header`, `Label`, `IndicatorText`, `RequiredIndicator`, `Description`, `ErrorMessage`, `Footer`, `CharacterCount`; `CharacterCount`: `current`, `max`; 상속 field primitive/label variant props | - | - | 미구현 | `Root`: 미구현 — Field.Root 행에서 props를 별도 관리한다. / `Header`: 미구현 — Field.Header 행에서 props를 별도 관리한다. / `Label`: 미구현 — Field.Label 행에서 props를 별도 관리한다. / `IndicatorText`: 미구현 — Field.IndicatorText 행에서 props를 별도 관리한다. / `RequiredIndicator`: 미구현 — Field.RequiredIndicator 행에서 props를 별도 관리한다. / `Footer`: 미구현 — Field.Footer 행에서 props를 별도 관리한다. / `Description`: 미구현 — Field.Description 행에서 props를 별도 관리한다. / `ErrorMessage`: 미구현 — Field.ErrorMessage 행에서 props를 별도 관리한다. / `CharacterCount`: 미구현 — Field.CharacterCount 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `Field.Root` | `FieldRootProps`: 상속 `FieldVariantProps`, `Field.RootProps` | - | - | 미구현 | `RootProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Field.Header` | `FieldHeaderProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `HeaderProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Field.Label` | `FieldLabelProps`: 상속 `FieldLabelVariantProps`, `Field.LabelProps` | - | - | 미구현 | `LabelProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Field.IndicatorText` | `FieldIndicatorTextProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 미구현 | `IndicatorTextProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Field.RequiredIndicator` | `FieldRequiredIndicatorProps`: 상속 `svg attrs` | - | - | 미구현 | `RequiredIndicatorProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Field.Footer` | `FieldFooterProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `FooterProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Field.Description` | `FieldDescriptionProps`: 상속 `Field.DescriptionProps` | - | - | 미구현 | `DescriptionProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Field.ErrorMessage` | `FieldErrorMessageProps`: 상속 `Field.ErrorMessageProps` | - | - | 미구현 | `ErrorMessageProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Field.CharacterCount` | `FieldCharacterCountProps`: `current`, `max`; 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `CharacterCountProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `FieldButton` | `Root`, `Header`, `Label`, `IndicatorText`, `RequiredIndicator`, `PrefixIcon`, `PrefixText`, `SuffixIcon`, `SuffixText`, `Footer`, `Description`, `ErrorMessage`, `HiddenInput`, `Button`, `Control`, `ClearButton`, `Value`, `Placeholder`; 상속 field button primitive props | - | - | 미구현 | `Button`: 미구현 — FieldButton.Button 행에서 props를 별도 관리한다. / `ClearButton`: 미구현 — FieldButton.ClearButton 행에서 props를 별도 관리한다. / `Description`: 미구현 — FieldButton.Description 행에서 props를 별도 관리한다. / `ErrorMessage`: 미구현 — FieldButton.ErrorMessage 행에서 props를 별도 관리한다. / `Root`: 미구현 — FieldButton.Root 행에서 props를 별도 관리한다. / `Footer`: 미구현 — FieldButton.Footer 행에서 props를 별도 관리한다. / `Header`: 미구현 — FieldButton.Header 행에서 props를 별도 관리한다. / `HiddenInput`: 제약 — FieldButton.HiddenInput 행에서 props를 별도 관리한다. / `IndicatorText`: 미구현 — FieldButton.IndicatorText 행에서 props를 별도 관리한다. / `Label`: 미구현 — FieldButton.Label 행에서 props를 별도 관리한다. / `Placeholder`: 미구현 — FieldButton.Placeholder 행에서 props를 별도 관리한다. / `PrefixIcon`: 미구현 — FieldButton.PrefixIcon 행에서 props를 별도 관리한다. / `PrefixText`: 미구현 — FieldButton.PrefixText 행에서 props를 별도 관리한다. / `RequiredIndicator`: 미구현 — FieldButton.RequiredIndicator 행에서 props를 별도 관리한다. / `Control`: 미구현 — FieldButton.Control 행에서 props를 별도 관리한다. / `SuffixIcon`: 미구현 — FieldButton.SuffixIcon 행에서 props를 별도 관리한다. / `SuffixText`: 미구현 — FieldButton.SuffixText 행에서 props를 별도 관리한다. / `Value`: 미구현 — FieldButton.Value 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `FieldButton.Button` | `FieldButtonButtonProps`: 상속 `FieldButton.ButtonProps` | - | - | 미구현 | `ButtonProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `FieldButton.ClearButton` | `FieldButtonClearButtonProps`: 상속 `FieldButton.ClearButtonProps` | - | - | 미구현 | `ClearButtonProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `FieldButton.Description` | `FieldButtonDescriptionProps`: 상속 `FieldButton.DescriptionProps` | - | - | 미구현 | `DescriptionProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `FieldButton.ErrorMessage` | `FieldButtonErrorMessageProps`: 상속 `FieldButton.ErrorMessageProps` | - | - | 미구현 | `ErrorMessageProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `FieldButton.Root` | `FieldButtonRootProps`: 상속 `FieldVariantProps`, `FieldButton.RootProps` | - | - | 미구현 | `RootProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `FieldButton.Footer` | `FieldButtonFooterProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `FooterProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `FieldButton.Header` | `FieldButtonHeaderProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `HeaderProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `FieldButton.HiddenInput` | `FieldButtonHiddenInputProps`: 상속 `FieldButton.HiddenInputProps` | - | - | 제약 | `HiddenInputProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `FieldButton.IndicatorText` | `FieldButtonIndicatorTextProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 미구현 | `IndicatorTextProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `FieldButton.Label` | `FieldButtonLabelProps`: 상속 `PrimitiveProps`, `FieldLabelVariantProps`, `div attrs` | - | - | 미구현 | `LabelProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `FieldButton.Placeholder` | `FieldButtonPlaceholderProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `PlaceholderProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `FieldButton.PrefixIcon` | `FieldButtonPrefixIconProps`: 상속 `InternalIconProps` | - | - | 미구현 | `PrefixIconProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `FieldButton.PrefixText` | `FieldButtonPrefixTextProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 미구현 | `PrefixTextProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `FieldButton.RequiredIndicator` | `FieldButtonRequiredIndicatorProps`: 상속 `svg attrs` | - | - | 미구현 | `RequiredIndicatorProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `FieldButton.Control` | `FieldButtonControlProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `ControlProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `FieldButton.SuffixIcon` | `FieldButtonSuffixIconProps`: 상속 `InternalIconProps` | - | - | 미구현 | `SuffixIconProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `FieldButton.SuffixText` | `FieldButtonSuffixTextProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 미구현 | `SuffixTextProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `FieldButton.Value` | `FieldButtonValueProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `ValueProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Fieldset` | `Root`, `Header`, `Label`, `IndicatorText`, `RequiredIndicator`, `Description`, `ErrorMessage`, `Footer`; 상속 fieldset primitive/label variant props | - | - | 미구현 | `Root`: 미구현 — Fieldset.Root 행에서 props를 별도 관리한다. / `Header`: 미구현 — Fieldset.Header 행에서 props를 별도 관리한다. / `Label`: 미구현 — Fieldset.Label 행에서 props를 별도 관리한다. / `IndicatorText`: 미구현 — Fieldset.IndicatorText 행에서 props를 별도 관리한다. / `RequiredIndicator`: 미구현 — Fieldset.RequiredIndicator 행에서 props를 별도 관리한다. / `Footer`: 미구현 — Fieldset.Footer 행에서 props를 별도 관리한다. / `Description`: 미구현 — Fieldset.Description 행에서 props를 별도 관리한다. / `ErrorMessage`: 미구현 — Fieldset.ErrorMessage 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `Fieldset.Root` | `FieldsetRootProps`: 상속 `FieldVariantProps`, `Fieldset.RootProps` | - | - | 미구현 | `RootProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Fieldset.Header` | `FieldsetHeaderProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `HeaderProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Fieldset.Label` | `FieldsetLabelProps`: 상속 `FieldLabelVariantProps`, `Fieldset.LabelProps` | - | - | 미구현 | `LabelProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Fieldset.IndicatorText` | `FieldsetIndicatorTextProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 미구현 | `IndicatorTextProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Fieldset.RequiredIndicator` | `FieldsetRequiredIndicatorProps`: 상속 `svg attrs` | - | - | 미구현 | `RequiredIndicatorProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Fieldset.Footer` | `FieldsetFooterProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `FooterProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Fieldset.Description` | `FieldsetDescriptionProps`: 상속 `Fieldset.DescriptionProps` | - | - | 미구현 | `DescriptionProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Fieldset.ErrorMessage` | `FieldsetErrorMessageProps`: 상속 `Fieldset.ErrorMessageProps` | - | - | 미구현 | `ErrorMessageProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Flex` | `FlexProps`: `display?`, `direction?`, `wrap?`, `align?`, `justify?`, `grow?`, `shrink?`; 상속 `BoxProps` | `Flex`, `HStack`, `VStack` | `Flex`: `direction?`, `spacing?`, 상속 `Omit<ComponentProps<typeof Row>, 'spacing'>`; `HStack`: `spacing?`, 상속 `Omit<ComponentProps<typeof Row>, 'spacing'>`; `VStack`: `spacing?`, 상속 `Omit<ComponentProps<typeof Column>, 'spacing'>` | 부분완료 | `display?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `direction?`: 완료 — Mossy 공개 props에서 동일 이름으로 제공한다. / `wrap?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `align?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `justify?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `grow?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `shrink?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `BoxProps`: 부분완료 — Mossy modifier 또는 일부 layout props로만 대응한다. / `FlexProps`: 부분완료 — Mossy modifier 또는 일부 layout props로만 대응한다. / `spacing?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. / `HStack`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. / `VStack`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 Expo UI `Row`/`Column` 기반 `direction`, `spacing` 중심이다. |
| `Float` | `FloatProps`: `as?`, `placement`, `offsetX?`, `offsetY?`, `zIndex?`; 상속 div attrs | `Float` | `Float`: `children?`, `placement`, `offsetX?`, `offsetY?`, `testID?`, `modifiers?` | 부분완료 | `as?`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `placement`: 완료 — Mossy 공개 props에서 동일 이름으로 제공한다. / `offsetX?`: 완료 — Mossy 공개 props에서 동일 이름으로 제공한다. / `offsetY?`: 완료 — Mossy 공개 props에서 동일 이름으로 제공한다. / `zIndex?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `div attrs`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `children?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. / `testID?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. / `modifiers?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 `placement`, `offsetX`, `offsetY`, `modifiers` 중심이다. DOM `as`는 제공하지 않는다. |
| `FloatingActionButton` | `Root`, `Icon`, `Label`; 상속 `FloatingActionButtonVariantProps`, `PrimitiveProps`, button/span attrs, `InternalIconProps` | - | - | 미구현 | `Root`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `Icon`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `Label`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `FloatingActionButtonVariantProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `PrimitiveProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `InternalIconProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `span attrs`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Expo UI platform FAB 가능성 확인 필요. |
| `Grid` | `GridProps`: `display?`, `align?`, `justify?`, `justifyItems?`, `columns?`, `rows?`, `autoFlow?`, `autoColumns?`, `autoRows?`; 상속 `BoxProps` | `Grid` | `Grid`: `children?`, `columns`, `horizontalSpacing?`, `verticalSpacing?`, `testID?`, `modifiers?` | 부분완료 | `display?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `align?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `justify?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `justifyItems?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `columns?`: 완료 — Mossy 공개 props에서 동일 이름으로 제공한다. / `rows?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `autoFlow?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `autoColumns?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `autoRows?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `BoxProps`: 부분완료 — Mossy modifier 또는 일부 layout props로만 대응한다. / `children?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. / `horizontalSpacing?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. / `verticalSpacing?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. / `testID?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. / `modifiers?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 `columns`, `horizontalSpacing`, `verticalSpacing` 중심이다. CSS grid 세부 props는 제공하지 않는다. |
| `GridItem` | `GridItemProps`: `asChild?`, `colSpan?`, `rowSpan?`, `colStart?`, `colEnd?`, `rowStart?`, `rowEnd?`; 상속 `BoxProps` | - | - | 미구현 | `asChild?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `colSpan?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `rowSpan?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `colStart?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `colEnd?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `rowStart?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `rowEnd?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `BoxProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy Grid item span API 추가 여부 확인 필요. |
| `HelpBubble` | `Root`: `placement?`, `gutter?`, `overflowPadding?`, `arrowPadding?`; `Anchor`, `Arrow`, `ArrowTip`, `Body`, `CloseButton`, `Content`, `Description`, `PositionerPortal`, `Positioner`, `Title`, `Trigger`; 상속 Popover primitive/HTML props | - | - | 제약 | `Anchor`: 제약 — HelpBubble.Anchor 행에서 props를 별도 관리한다. / `Arrow`: 제약 — HelpBubble.Arrow 행에서 props를 별도 관리한다. / `ArrowTip`: 제약 — HelpBubble.ArrowTip 행에서 props를 별도 관리한다. / `Body`: 제약 — HelpBubble.Body 행에서 props를 별도 관리한다. / `CloseButton`: 제약 — HelpBubble.CloseButton 행에서 props를 별도 관리한다. / `Content`: 제약 — HelpBubble.Content 행에서 props를 별도 관리한다. / `Description`: 제약 — HelpBubble.Description 행에서 props를 별도 관리한다. / `Positioner`: 제약 — HelpBubble.Positioner 행에서 props를 별도 관리한다. / `PositionerPortal`: 제약 — HelpBubble.PositionerPortal 행에서 props를 별도 관리한다. / `Root`: 제약 — HelpBubble.Root 행에서 props를 별도 관리한다. / `Title`: 제약 — HelpBubble.Title 행에서 props를 별도 관리한다. / `Trigger`: 제약 — HelpBubble.Trigger 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `HelpBubble.Anchor` | `HelpBubbleAnchorProps`: 상속 `PopoverPrimitive.AnchorProps` | - | - | 제약 | `AnchorProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `HelpBubble.Arrow` | `HelpBubbleArrowProps`: 상속 `PopoverPrimitive.ArrowProps` | - | - | 제약 | `ArrowProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `HelpBubble.ArrowTip` | `HelpBubbleArrowTipProps`: `tipRadius?`; 상속 `svg attrs` | - | - | 제약 | `ArrowTipProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `HelpBubble.Body` | `HelpBubbleBodyProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 제약 | `BodyProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `HelpBubble.CloseButton` | `HelpBubbleCloseButtonProps`: 상속 `PopoverPrimitive.CloseButtonProps` | - | - | 제약 | `CloseButtonProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `HelpBubble.Content` | `HelpBubbleContentProps`: 상속 `PrimitiveProps`, `Pick<StyleProps, "maxWidth">`, `div attrs` | - | - | 제약 | `ContentProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `HelpBubble.Description` | `HelpBubbleDescriptionProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 제약 | `DescriptionProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `HelpBubble.Positioner` | `HelpBubblePositionerProps`: 상속 `PopoverPrimitive.PositionerProps` | - | - | 제약 | `PositionerProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `HelpBubble.PositionerPortal` | `HelpBubblePositionerPortalProps`: 상속 `PopoverPrimitive.PositionerPortalProps` | - | - | 제약 | `PositionerPortalProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `HelpBubble.Root` | `HelpBubbleRootProps`: `placement?`, `gutter?`, `overflowPadding?`, `arrowPadding?`; 상속 `HelpBubbleVariantProps`, `PopoverPrimitive.RootProps` | - | - | 제약 | `RootProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `HelpBubble.Title` | `HelpBubbleTitleProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 제약 | `TitleProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `HelpBubble.Trigger` | `HelpBubbleTriggerProps`: 상속 `PopoverPrimitive.TriggerProps` | - | - | 제약 | `TriggerProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Icon` | `IconProps`: `svg`, `size?`, `color?`; `PrefixIconProps`: `svg`; `SuffixIconProps`: `svg` | `Icon` | `Icon`: `color?`, `size?`, 상속 `Omit<ComponentProps<typeof ExpoIcon>, 'color' \| 'size'>` | 부분완료 | `svg`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `size?`: 완료 — Mossy 공개 props에서 동일 이름으로 제공한다. / `color?`: 완료 — Mossy 공개 props에서 동일 이름으로 제공한다. / `PrefixIconProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `SuffixIconProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 Expo UI Icon pass-through에 `color`, `size` 토큰 prop을 주입한다. |
| `IdentityPlaceholder` | `Root`: 상속 `IdentityPlaceholderVariantProps`, `PrimitiveProps`, div attrs; `Image`: SVG props | - | - | 미구현 | `Root`: 미구현 — IdentityPlaceholder.Root 행에서 props를 별도 관리한다. / `Image`: 미구현 — IdentityPlaceholder.Image 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `IdentityPlaceholder.Root` | `IdentityPlaceholderRootProps`: 상속 `IdentityPlaceholderVariantProps`, `PrimitiveProps`, `div attrs` | - | - | 미구현 | `RootProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `IdentityPlaceholder.Image` | `IdentityPlaceholderImageProps`: 상속 `svg attrs` | - | - | 미구현 | `ImageProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `ImageFrame` | `ImageFrameProps`: `rounded?`, `src`, `alt`, `fallback?`, `loading?`, `decoding?`, `crossOrigin?`, `referrerPolicy?`, `sizes?`, `srcSet?`, `onLoad?`, `onError?`, `children?`; `Floater`, `Badge`, `Icon`, `Indicator`, `ReactionButton` | - | - | 제약 | `rounded?`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `src`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `alt`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `fallback?`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `loading?`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `decoding?`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `crossOrigin?`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `referrerPolicy?`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `sizes?`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `srcSet?`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `onLoad?`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `onError?`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `children?`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `Floater`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `Badge`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `Icon`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `Indicator`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `ReactionButton`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | Web image attributes와 responsive image props가 RN Image API와 다르다. |
| `Inline` | `InlineProps`: 상속 `BoxProps` | - | - | 미구현 | `BoxProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | HStack/Flex로 대응 가능하나 별도 공개 API 없음. |
| `InlineBanner` | `Root`, `Content`, `Title`, `Description`, `Link`, `CloseButton`; 상속 `InlineBannerVariantProps`, `DismissibleRootProps`, `PrimitiveProps`, HTML/button attrs | - | - | 미구현 | `CloseButton`: 미구현 — InlineBanner.CloseButton 행에서 props를 별도 관리한다. / `Content`: 미구현 — InlineBanner.Content 행에서 props를 별도 관리한다. / `Description`: 미구현 — InlineBanner.Description 행에서 props를 별도 관리한다. / `Link`: 미구현 — InlineBanner.Link 행에서 props를 별도 관리한다. / `Root`: 미구현 — InlineBanner.Root 행에서 props를 별도 관리한다. / `Title`: 미구현 — InlineBanner.Title 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `InlineBanner.CloseButton` | `InlineBannerCloseButtonProps`: 상속 `PrimitiveProps`, `button attrs` | - | - | 미구현 | `CloseButtonProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `InlineBanner.Content` | `InlineBannerContentProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `ContentProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `InlineBanner.Description` | `InlineBannerDescriptionProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 미구현 | `DescriptionProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `InlineBanner.Link` | `InlineBannerLinkProps`: 상속 `PrimitiveProps`, `button attrs` | - | - | 미구현 | `LinkProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `InlineBanner.Root` | `InlineBannerRootProps`: 상속 `InlineBannerVariantProps`, `DismissibleRootProps` | - | - | 미구현 | `RootProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `InlineBanner.Title` | `InlineBannerTitleProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 미구현 | `TitleProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `LinkContent` | `LinkContentProps`: 상속 `LinkContentVariantProps`, `PrimitiveProps`, `StyleProps.color`, span attrs | - | - | 미구현 | `LinkContentVariantProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `PrimitiveProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `StyleProps.color`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `span attrs`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Link/pressable 텍스트 API 필요. |
| `List` | `Root`: `itemBorderRadius?`; `Header`: `as?`; `Item`, `Content`, `Title`, `Detail`, `Prefix`, `Suffix`; 상속 `VStackProps`, `ListHeaderVariantProps`, `ListItemVariantProps`, `StyleProps`, HTML attrs | `List`, `ListHeader`, `ListItem` | `List`: `ComponentProps<typeof ExpoList>`; `ListHeader`: `title`, `variant?`, `children?`, `testID?`, `modifiers?`; `ListItem`: `title?`, `detail?`, 상속 `ComponentProps<typeof ExpoListItem>` | 부분완료 | `Content`: 미구현 — List.Content 행에서 props를 별도 관리한다. / `Detail`: 부분완료 — List.Detail 행에서 props를 별도 관리한다. / `Item`: 부분완료 — List.Item 행에서 props를 별도 관리한다. / `Prefix`: 미구현 — List.Prefix 행에서 props를 별도 관리한다. / `Root`: 부분완료 — List.Root 행에서 props를 별도 관리한다. / `Suffix`: 미구현 — List.Suffix 행에서 props를 별도 관리한다. / `Title`: 부분완료 — List.Title 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `List.Content` | `ListContentProps`: 상속 `PrimitiveProps`, `Pick<StyleProps, "gap" \| "pr" \| "paddingRight">`, `div attrs` | - | - | 미구현 | `ContentProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `List.Detail` | `ListDetailProps`: 상속 `PrimitiveProps`, `div attrs` | `ListItem` | `detail?` | 부분완료 | `DetailProps`: 부분완료 — `ListItem.detail` 데이터 prop으로 대응한다. | Seed part props와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | `ListItem.detail` 데이터 prop으로 대응한다. |
| `List.Item` | `ListItemProps`: 상속 `PrimitiveProps`, `Pick<StyleProps, "alignItems">`, `li attrs`, `ListItemVariantProps` | `ListItem` | `title?`, `detail?`, 상속 `ComponentProps<typeof ExpoListItem>` | 부분완료 | `ItemProps`: 부분완료 — Expo UI `ListItem`과 닫힌 데이터 props로 대응한다. | Seed part props와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Expo UI `ListItem`과 닫힌 데이터 props로 대응한다. |
| `List.Prefix` | `ListPrefixProps`: 상속 `PrimitiveProps`, `Pick<StyleProps, "pr" \| "paddingRight">`, `div attrs` | - | - | 미구현 | `PrefixProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `List.Root` | `ListRootProps`: `itemBorderRadius?`; 상속 `VStackProps` | `List` | `ComponentProps<typeof ExpoList>` | 부분완료 | `RootProps`: 부분완료 — Expo UI `List`로 root container를 대응한다. | Seed part props와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Expo UI `List`로 root container를 대응한다. |
| `List.Suffix` | `ListSuffixProps`: 상속 `PrimitiveProps`, `Pick<StyleProps, "gap" \| "position">`, `div attrs` | - | - | 미구현 | `SuffixProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `List.Title` | `ListTitleProps`: 상속 `PrimitiveProps`, `div attrs` | `ListItem` | `title?` | 부분완료 | `TitleProps`: 부분완료 — `ListItem.title` 데이터 prop으로 대응한다. | Seed part props와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | `ListItem.title` 데이터 prop으로 대응한다. |
| `LoadingIndicator` | `LoadingIndicatorProps`: `indicator`; 상속 span attrs | `LoadingIndicator` | `LoadingIndicator`: 상속 `Omit<MossyProgressCircleProps, 'value'>` | 부분완료 | `indicator`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `span attrs`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 `ProgressCircle` 기반 indeterminate 래퍼다. |
| `MannerTemp` | `MannerTempProps`: 상속 `MannerTempVariantProps`, `PrimitiveProps`, span attrs; `MannerTempEmoteProps`: `level?` | - | - | 미구현 | `MannerTempVariantProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `PrimitiveProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `MannerTempEmoteProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `level?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `span attrs`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 도메인 특화 표시 컴포넌트. |
| `MannerTempBadge` | `MannerTempBadgeProps`: 상속 `MannerTempBadgeVariantProps`, `PrimitiveProps`, span attrs | - | - | 미구현 | `MannerTempBadgeVariantProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `PrimitiveProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `span attrs`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 도메인 특화 표시 컴포넌트. |
| `MenuSheet` | `Root`: `lazyMount?`, `unmountOnExit?`; `Backdrop`, `CloseButton`, `Content`, `Description`, `Footer`, `Group`, `Header`, `Item`, `ItemContent`, `ItemDescription`, `ItemLabel`, `List`, `Positioner`, `Title`, `Trigger`; 상속 Dialog primitive/HTML props | - | - | 제약 | `Backdrop`: 제약 — MenuSheet.Backdrop 행에서 props를 별도 관리한다. / `Positioner`: 제약 — MenuSheet.Positioner 행에서 props를 별도 관리한다. / `Content`: 제약 — MenuSheet.Content 행에서 props를 별도 관리한다. / `Footer`: 제약 — MenuSheet.Footer 행에서 props를 별도 관리한다. / `Header`: 제약 — MenuSheet.Header 행에서 props를 별도 관리한다. / `Root`: 제약 — MenuSheet.Root 행에서 props를 별도 관리한다. / `Title`: 제약 — MenuSheet.Title 행에서 props를 별도 관리한다. / `Description`: 제약 — MenuSheet.Description 행에서 props를 별도 관리한다. / `Trigger`: 제약 — MenuSheet.Trigger 행에서 props를 별도 관리한다. / `List`: 제약 — MenuSheet.List 행에서 props를 별도 관리한다. / `Group`: 제약 — MenuSheet.Group 행에서 props를 별도 관리한다. / `Item`: 제약 — MenuSheet.Item 행에서 props를 별도 관리한다. / `ItemContent`: 제약 — MenuSheet.ItemContent 행에서 props를 별도 관리한다. / `ItemLabel`: 제약 — MenuSheet.ItemLabel 행에서 props를 별도 관리한다. / `ItemDescription`: 제약 — MenuSheet.ItemDescription 행에서 props를 별도 관리한다. / `CloseButton`: 제약 — MenuSheet.CloseButton 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `MenuSheet.Backdrop` | `MenuSheetBackdropProps`: 상속 `DialogPrimitive.BackdropProps` | - | - | 제약 | `BackdropProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `MenuSheet.Positioner` | `MenuSheetPositionerProps`: 상속 `DialogPrimitive.PositionerProps` | - | - | 제약 | `PositionerProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `MenuSheet.Content` | `MenuSheetContentProps`: 상속 `DialogPrimitive.ContentProps`, `Pick<MenuSheetItemVariantProps, "labelAlign">` | - | - | 제약 | `ContentProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `MenuSheet.Footer` | `MenuSheetFooterProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 제약 | `FooterProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `MenuSheet.Header` | `MenuSheetHeaderProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 제약 | `HeaderProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `MenuSheet.Root` | `MenuSheetRootProps`: `lazyMount?`, `unmountOnExit?`; 상속 `MenuSheetVariantProps`, `DialogPrimitive.RootProps` | - | - | 제약 | `RootProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `MenuSheet.Title` | `MenuSheetTitleProps`: 상속 `DialogPrimitive.TitleProps` | - | - | 제약 | `TitleProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `MenuSheet.Description` | `MenuSheetDescriptionProps`: 상속 `DialogPrimitive.DescriptionProps` | - | - | 제약 | `DescriptionProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `MenuSheet.Trigger` | `MenuSheetTriggerProps`: 상속 `DialogPrimitive.TriggerProps` | - | - | 제약 | `TriggerProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `MenuSheet.List` | `MenuSheetListProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 제약 | `ListProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `MenuSheet.Group` | `MenuSheetGroupProps`: 상속 `div attrs`, `Pick<MenuSheetItemVariantProps, "labelAlign">` | - | - | 제약 | `GroupProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `MenuSheet.Item` | `MenuSheetItemProps`: 상속 `PrimitiveProps`, `MenuSheetItemVariantProps`, `button attrs` | - | - | 제약 | `ItemProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `MenuSheet.ItemContent` | `MenuSheetItemContentProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 제약 | `ItemContentProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `MenuSheet.ItemLabel` | `MenuSheetItemLabelProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 제약 | `ItemLabelProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `MenuSheet.ItemDescription` | `MenuSheetItemDescriptionProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 제약 | `ItemDescriptionProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `MenuSheet.CloseButton` | `MenuSheetCloseButtonProps`: 상속 `DialogPrimitive.CloseButtonProps` | - | - | 제약 | `CloseButtonProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `NotificationBadge` | `NotificationBadgeProps`, `NotificationBadgePositionerProps`: 상속 notification badge variant props, `PrimitiveProps`, span attrs | - | - | 미구현 | `NotificationBadgeProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `NotificationBadgePositionerProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `PrimitiveProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `span attrs`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Badge 계열과 함께 검토. |
| `PageBanner` | `Root`, `Content`, `Body`, `Title`, `Description`, `Button`, `CloseButton`; 상속 `PageBannerVariantProps`, `DismissibleRootProps`, `PrimitiveProps`, HTML/button attrs | - | - | 미구현 | `Body`: 미구현 — PageBanner.Body 행에서 props를 별도 관리한다. / `CloseButton`: 미구현 — PageBanner.CloseButton 행에서 props를 별도 관리한다. / `Content`: 미구현 — PageBanner.Content 행에서 props를 별도 관리한다. / `Description`: 미구현 — PageBanner.Description 행에서 props를 별도 관리한다. / `Button`: 미구현 — PageBanner.Button 행에서 props를 별도 관리한다. / `Root`: 미구현 — PageBanner.Root 행에서 props를 별도 관리한다. / `Title`: 미구현 — PageBanner.Title 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `PageBanner.Body` | `PageBannerBodyProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `BodyProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `PageBanner.CloseButton` | `PageBannerCloseButtonProps`: 상속 `PrimitiveProps`, `button attrs` | - | - | 미구현 | `CloseButtonProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `PageBanner.Content` | `PageBannerContentProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `ContentProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `PageBanner.Description` | `PageBannerDescriptionProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 미구현 | `DescriptionProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `PageBanner.Button` | `PageBannerButtonProps`: 상속 `PrimitiveProps`, `button attrs` | - | - | 미구현 | `ButtonProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `PageBanner.Root` | `PageBannerRootProps`: 상속 `PageBannerVariantProps`, `DismissibleRootProps` | - | - | 미구현 | `RootProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `PageBanner.Title` | `PageBannerTitleProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 미구현 | `TitleProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Portal` | `PortalProps`: `@seed-design/react-portal`에서 재노출 | - | - | 제약 | `@seed-design/react-portal`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | Web portal API다. Expo UI Host/네이티브 modal 계층과 동일하게 제공하지 않는다. |
| `ProgressCircle` | `Root`, `Track`, `Range`; 상속 `ProgressCirclePrimitive.*Props`, `ProgressCircleVariantProps` | `ProgressCircle` | `ProgressCircle`: `value?`, `color?`, `trackColor?`, `testID?`, `modifiers?` | 부분완료 | `Root`: 부분완료 — ProgressCircle.Root 행에서 props를 별도 관리한다. / `Track`: 부분완료 — ProgressCircle.Track 행에서 props를 별도 관리한다. / `Range`: 부분완료 — ProgressCircle.Range 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `ProgressCircle.Root` | `ProgressCircleRootProps`: 상속 `ProgressCirclePrimitive.RootProps`, `Omit<ProgressCircleVariantProps, "indeterminate">` | `ProgressCircle` | `value?`, `color?`, `trackColor?`, `testID?`, `modifiers?` | 부분완료 | `RootProps`: 부분완료 — 단일 ProgressCircle 컴포넌트로 root와 value를 대응한다. | Seed part props와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | 단일 ProgressCircle 컴포넌트로 root와 value를 대응한다. |
| `ProgressCircle.Track` | `ProgressCircleTrackProps`: 상속 `ProgressCirclePrimitive.TrackProps` | `ProgressCircle` | `trackColor?` | 부분완료 | `TrackProps`: 부분완료 — 분리 part 대신 `trackColor` 데이터 prop으로 대응한다. | Seed part props와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | 분리 part 대신 `trackColor` 데이터 prop으로 대응한다. |
| `ProgressCircle.Range` | `ProgressCircleRangeProps`: 상속 `ProgressCirclePrimitive.RangeProps` | `ProgressCircle` | `color?` | 부분완료 | `RangeProps`: 부분완료 — 분리 part 대신 `color` 데이터 prop으로 대응한다. | Seed part props와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | 분리 part 대신 `color` 데이터 prop으로 대응한다. |
| `PullToRefresh` | `Root`, `Content`, `Indicator`; 상속 pull-to-refresh primitive props, `PullToRefreshVariantProps` | - | - | 미구현 | `Content`: 미구현 — PullToRefresh.Content 행에서 props를 별도 관리한다. / `Indicator`: 미구현 — PullToRefresh.Indicator 행에서 props를 별도 관리한다. / `Root`: 미구현 — PullToRefresh.Root 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `PullToRefresh.Content` | `PullToRefreshContentProps`: 상속 `PullToRefreshPrimitive.ContentProps` | - | - | 미구현 | `ContentProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `PullToRefresh.Indicator` | `PullToRefreshIndicatorProps`: 상속 `PullToRefreshPrimitive.IndicatorProps` | - | - | 미구현 | `IndicatorProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `PullToRefresh.Root` | `PullToRefreshRootProps`: 상속 `PullToRefreshVariantProps`, `PullToRefreshPrimitive.RootProps` | - | - | 미구현 | `RootProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `RadioGroup` | `Root`, `Item`, `ItemControl`, `ItemHiddenInput`, `ItemIndicator`, `ItemLabel`; `ItemIndicator`: `unchecked?`, `checked?`; 상속 radio group primitive/variant props | - | - | 미구현 | `Root`: 미구현 — RadioGroup.Root 행에서 props를 별도 관리한다. / `Item`: 미구현 — RadioGroup.Item 행에서 props를 별도 관리한다. / `ItemLabel`: 미구현 — RadioGroup.ItemLabel 행에서 props를 별도 관리한다. / `ItemControl`: 미구현 — RadioGroup.ItemControl 행에서 props를 별도 관리한다. / `ItemIndicator`: 미구현 — RadioGroup.ItemIndicator 행에서 props를 별도 관리한다. / `ItemHiddenInput`: 제약 — RadioGroup.ItemHiddenInput 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `RadioGroup.Root` | `RadioGroupRootProps`: 상속 `RadioGroupVariantProps`, `PrimitiveProps`, `div attrs` | - | - | 미구현 | `RootProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `RadioGroup.Item` | `RadioGroupItemProps`: 상속 `RadioVariantProps`, `RadiomarkVariantProps`, `RadioGroupPrimitive.ItemProps` | - | - | 미구현 | `ItemProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `RadioGroup.ItemLabel` | `RadioGroupItemLabelProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 미구현 | `ItemLabelProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `RadioGroup.ItemControl` | `RadioGroupItemControlProps`: 상속 `RadiomarkVariantProps`, `RadioGroupPrimitive.ItemControlProps` | - | - | 미구현 | `ItemControlProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `RadioGroup.ItemIndicator` | `RadioGroupItemIndicatorProps`: `unchecked?`, `checked?`; 상속 `svg attrs` | - | - | 미구현 | `ItemIndicatorProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `RadioGroup.ItemHiddenInput` | `RadioGroupItemHiddenInputProps`: 상속 `RadioGroupPrimitive.ItemHiddenInputProps` | - | - | 제약 | `ItemHiddenInputProps`: 제약 — RN/Expo UI 레이어에서 DOM hidden input contract를 제공하지 않는다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `RadioGroupField` | `Root`, `Header`, `Label`, `IndicatorText`, `RequiredIndicator`, `Description`, `ErrorMessage`, `Footer`; 상속 field/radio primitive props | - | - | 미구현 | `Root`: 미구현 — RadioGroupField.Root 행에서 props를 별도 관리한다. / `Header`: 미구현 — RadioGroupField.Header 행에서 props를 별도 관리한다. / `Label`: 미구현 — RadioGroupField.Label 행에서 props를 별도 관리한다. / `IndicatorText`: 미구현 — RadioGroupField.IndicatorText 행에서 props를 별도 관리한다. / `RequiredIndicator`: 미구현 — RadioGroupField.RequiredIndicator 행에서 props를 별도 관리한다. / `Footer`: 미구현 — RadioGroupField.Footer 행에서 props를 별도 관리한다. / `Description`: 미구현 — RadioGroupField.Description 행에서 props를 별도 관리한다. / `ErrorMessage`: 미구현 — RadioGroupField.ErrorMessage 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `RadioGroupField.Root` | `RadioGroupFieldRootProps`: 상속 `FieldVariantProps`, `RadioGroupPrimitive.RootProps` | - | - | 미구현 | `RootProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `RadioGroupField.Header` | `RadioGroupFieldHeaderProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `HeaderProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `RadioGroupField.Label` | `RadioGroupFieldLabelProps`: 상속 `FieldLabelVariantProps`, `RadioGroupPrimitive.LabelProps` | - | - | 미구현 | `LabelProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `RadioGroupField.IndicatorText` | `RadioGroupFieldIndicatorTextProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 미구현 | `IndicatorTextProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `RadioGroupField.RequiredIndicator` | `RadioGroupFieldRequiredIndicatorProps`: 상속 `svg attrs` | - | - | 미구현 | `RequiredIndicatorProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `RadioGroupField.Footer` | `RadioGroupFieldFooterProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `FooterProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `RadioGroupField.Description` | `RadioGroupFieldDescriptionProps`: 상속 `RadioGroupPrimitive.DescriptionProps` | - | - | 미구현 | `DescriptionProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `RadioGroupField.ErrorMessage` | `RadioGroupFieldErrorMessageProps`: 상속 `RadioGroupPrimitive.ErrorMessageProps` | - | - | 미구현 | `ErrorMessageProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `ReactionButton` | `ReactionButtonProps`: 상속 `ReactionButtonVariantProps`, `UsePendingButtonProps`, toggle primitive props | - | - | 미구현 | `ReactionButtonVariantProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `UsePendingButtonProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `toggle primitive props`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | ToggleButton/FAB 계열과 함께 검토. |
| `ResponsivePair` | `ResponsivePairProps`: `wrap?`, `children`; 상속 `FlexProps` | - | - | 미구현 | `wrap?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `children`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `FlexProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | RN 반응형 기준과 breakpoint 정책 필요. |
| `ScrollFog` | `ScrollFogProps`: `placement?`, `size?`, `sizes?`; 상속 `ScrollFogVariantProps`, div attrs | `ScrollFog` | `ScrollFog`: `children?`, `placement?`, `size?`, `sizes?`, 상속 `ViewProps` | 부분완료 | `placement?`: 완료 — Mossy 공개 props에서 동일 이름으로 제공한다. / `size?`: 완료 — Mossy 공개 props에서 동일 이름으로 제공한다. / `sizes?`: 완료 — Mossy 공개 props에서 동일 이름으로 제공한다. / `ScrollFogVariantProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `div attrs`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `children?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 RN `ViewProps`와 토큰 size를 사용한다. |
| `SegmentedControl` | `Root`, `Item`, `ItemHiddenInput`, `Indicator`; 상속 segmented control primitive/variant props | `SegmentedControl` | `SegmentedControl`: `options`, `selectedIndex`, `onSelectedIndexChange`, `disabled?`, `testID?`, `modifiers?` | 부분완료 | `Indicator`: 미구현 — SegmentedControl.Indicator 행에서 props를 별도 관리한다. / `Root`: 부분완료 — SegmentedControl.Root 행에서 props를 별도 관리한다. / `Item`: 부분완료 — SegmentedControl.Item 행에서 props를 별도 관리한다. / `ItemHiddenInput`: 제약 — SegmentedControl.ItemHiddenInput 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `SegmentedControl.Indicator` | `SegmentedControlIndicatorProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `IndicatorProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `SegmentedControl.Root` | `SegmentedControlRootProps`: 상속 `SegmentedControlVariantProps`, `SegmentedControlPrimitive.RootProps` | `SegmentedControl` | `options`, `selectedIndex`, `onSelectedIndexChange`, `disabled?`, `testID?`, `modifiers?` | 부분완료 | `RootProps`: 부분완료 — 닫힌 데이터 props로 root 상태와 items를 대응한다. | Seed part props와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | 닫힌 데이터 props로 root 상태와 items를 대응한다. |
| `SegmentedControl.Item` | `SegmentedControlItemProps`: 상속 `SegmentedControlPrimitive.ItemProps` | `SegmentedControl` | `options` | 부분완료 | `ItemProps`: 부분완료 — 분리 item part 대신 `options` 데이터 prop으로 대응한다. | Seed part props와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | 분리 item part 대신 `options` 데이터 prop으로 대응한다. |
| `SegmentedControl.ItemHiddenInput` | `SegmentedControlItemHiddenInputProps`: 상속 `SegmentedControlPrimitive.ItemHiddenInputProps` | - | - | 제약 | `ItemHiddenInputProps`: 제약 — RN/Expo UI 레이어에서 DOM hidden input contract를 제공하지 않는다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `SelectBox.CheckSelectBox` | namespace: `Body`, `CheckmarkControl`, `CheckmarkIcon`, `Content`, `Description`, `Footer`, `Group`, `HiddenInput`, `Label`, `Root`, `Trigger` | - | part별 행 참조 | 미구현 | `Body`: 미구현 — SelectBox.CheckSelectBox.Body 행에서 props를 별도 관리한다. / `CheckmarkControl`: 미구현 — SelectBox.CheckSelectBox.CheckmarkControl 행에서 props를 별도 관리한다. / `CheckmarkIcon`: 미구현 — SelectBox.CheckSelectBox.CheckmarkIcon 행에서 props를 별도 관리한다. / `Content`: 미구현 — SelectBox.CheckSelectBox.Content 행에서 props를 별도 관리한다. / `Description`: 미구현 — SelectBox.CheckSelectBox.Description 행에서 props를 별도 관리한다. / `Footer`: 미구현 — SelectBox.CheckSelectBox.Footer 행에서 props를 별도 관리한다. / `Group`: 미구현 — SelectBox.CheckSelectBox.Group 행에서 props를 별도 관리한다. / `HiddenInput`: 제약 — SelectBox.CheckSelectBox.HiddenInput 행에서 props를 별도 관리한다. / `Label`: 미구현 — SelectBox.CheckSelectBox.Label 행에서 props를 별도 관리한다. / `Root`: 미구현 — SelectBox.CheckSelectBox.Root 행에서 props를 별도 관리한다. / `Trigger`: 미구현 — SelectBox.CheckSelectBox.Trigger 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `SelectBox.CheckSelectBox.Body` | `CheckSelectBoxBodyProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `BodyProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `SelectBox.CheckSelectBox.CheckmarkControl` | `CheckSelectBoxCheckmarkControlProps`: 상속 `SelectBoxCheckmarkVariantProps`, `CheckboxPrimitive.ControlProps` | - | - | 미구현 | `CheckmarkControlProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `SelectBox.CheckSelectBox.CheckmarkIcon` | `CheckSelectBoxCheckmarkIconProps`: 상속 `InternalIconProps` | - | - | 미구현 | `CheckmarkIconProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `SelectBox.CheckSelectBox.Content` | `CheckSelectBoxContentProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `ContentProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `SelectBox.CheckSelectBox.Description` | `CheckSelectBoxDescriptionProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `DescriptionProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `SelectBox.CheckSelectBox.Footer` | `CheckSelectBoxFooterProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `FooterProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `SelectBox.CheckSelectBox.Group` | `CheckSelectBoxGroupProps`: `columns?`; 상속 `SelectBoxGroupVariantProps`, `PrimitiveProps`, `div attrs` | - | - | 미구현 | `GroupProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `SelectBox.CheckSelectBox.HiddenInput` | `CheckSelectBoxHiddenInputProps`: 상속 `CheckboxPrimitive.HiddenInputProps` | - | - | 제약 | `HiddenInputProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `SelectBox.CheckSelectBox.Label` | `CheckSelectBoxLabelProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `LabelProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `SelectBox.CheckSelectBox.Root` | `CheckSelectBoxRootProps`: `footerVisibility?`; 상속 `SelectBoxVariantProps`, `CheckboxPrimitive.RootProps` | - | - | 미구현 | `RootProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `SelectBox.CheckSelectBox.Trigger` | `CheckSelectBoxTriggerProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `TriggerProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `SelectBox.RadioSelectBox` | namespace: `Body`, `Content`, `Description`, `Footer`, `Group`, `HiddenInput`, `Item`, `Label`, `Trigger` | - | part별 행 참조 | 미구현 | `Body`: 미구현 — SelectBox.RadioSelectBox.Body 행에서 props를 별도 관리한다. / `Content`: 미구현 — SelectBox.RadioSelectBox.Content 행에서 props를 별도 관리한다. / `Description`: 미구현 — SelectBox.RadioSelectBox.Description 행에서 props를 별도 관리한다. / `Footer`: 미구현 — SelectBox.RadioSelectBox.Footer 행에서 props를 별도 관리한다. / `Group`: 미구현 — SelectBox.RadioSelectBox.Group 행에서 props를 별도 관리한다. / `HiddenInput`: 제약 — SelectBox.RadioSelectBox.HiddenInput 행에서 props를 별도 관리한다. / `Item`: 미구현 — SelectBox.RadioSelectBox.Item 행에서 props를 별도 관리한다. / `Label`: 미구현 — SelectBox.RadioSelectBox.Label 행에서 props를 별도 관리한다. / `Trigger`: 미구현 — SelectBox.RadioSelectBox.Trigger 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `SelectBox.RadioSelectBox.Body` | `RadioSelectBoxBodyProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `BodyProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `SelectBox.RadioSelectBox.Content` | `RadioSelectBoxContentProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `ContentProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `SelectBox.RadioSelectBox.Description` | `RadioSelectBoxDescriptionProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `DescriptionProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `SelectBox.RadioSelectBox.Footer` | `RadioSelectBoxFooterProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `FooterProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `SelectBox.RadioSelectBox.Group` | `RadioSelectBoxGroupProps`: `columns?`; 상속 `SelectBoxGroupVariantProps`, `PrimitiveProps`, `div attrs` | - | - | 미구현 | `GroupProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `SelectBox.RadioSelectBox.HiddenInput` | `RadioSelectBoxHiddenInputProps`: 상속 `RadioGroupPrimitive.ItemHiddenInputProps` | - | - | 제약 | `HiddenInputProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `SelectBox.RadioSelectBox.Item` | `RadioSelectBoxItemProps`: `footerVisibility?`; 상속 `SelectBoxVariantProps`, `RadioGroupPrimitive.ItemProps` | - | - | 미구현 | `ItemProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `SelectBox.RadioSelectBox.Label` | `RadioSelectBoxLabelProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `LabelProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `SelectBox.RadioSelectBox.Trigger` | `RadioSelectBoxTriggerProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `TriggerProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Skeleton` | `SkeletonProps`: 상속 `SkeletonVariantProps`, `PrimitiveProps`, `StyleProps.height/width`, div attrs | - | - | 미구현 | `SkeletonVariantProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `PrimitiveProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `StyleProps.height/width`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `div attrs`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Loading 문서 기준으로 구현 가능성 확인 필요. |
| `Slider` | `Root`, `Control`, `Track`, `Range`, `Thumb`, `HiddenInput`, `Markers`, `Marker`, `Tick`, `ValueIndicatorRoot`, `ValueIndicatorLabel`, `ValueIndicatorArrow`, `ValueIndicatorArrowTip`; `ArrowTip`: `tipRadius?`; 상속 slider primitive/variant props | `Slider` | `Slider`: `ComponentProps<typeof ExpoSlider>` | 부분완료 | `Root`: 부분완료 — Slider.Root 행에서 props를 별도 관리한다. / `Control`: 미구현 — Slider.Control 행에서 props를 별도 관리한다. / `Track`: 미구현 — Slider.Track 행에서 props를 별도 관리한다. / `Range`: 미구현 — Slider.Range 행에서 props를 별도 관리한다. / `Thumb`: 미구현 — Slider.Thumb 행에서 props를 별도 관리한다. / `HiddenInput`: 제약 — Slider.HiddenInput 행에서 props를 별도 관리한다. / `Tick`: 미구현 — Slider.Tick 행에서 props를 별도 관리한다. / `Markers`: 미구현 — Slider.Markers 행에서 props를 별도 관리한다. / `Marker`: 미구현 — Slider.Marker 행에서 props를 별도 관리한다. / `ValueIndicatorRoot`: 미구현 — Slider.ValueIndicatorRoot 행에서 props를 별도 관리한다. / `ValueIndicatorArrow`: 미구현 — Slider.ValueIndicatorArrow 행에서 props를 별도 관리한다. / `ValueIndicatorArrowTip`: 미구현 — Slider.ValueIndicatorArrowTip 행에서 props를 별도 관리한다. / `ValueIndicatorLabel`: 미구현 — Slider.ValueIndicatorLabel 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `Slider.Root` | `SliderRootProps`: 상속 `SliderVariantProps`, `Slider.RootProps` | `Slider` | `ComponentProps<typeof ExpoSlider>` | 부분완료 | `RootProps`: 부분완료 — universal `ExpoSlider` 단일 래퍼로 root 입력 동작만 대응한다. | Seed part props와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | universal `ExpoSlider` 단일 래퍼로 root 입력 동작만 대응한다. |
| `Slider.Control` | `SliderControlProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `ControlProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Slider.Track` | `SliderTrackProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `TrackProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Slider.Range` | `SliderRangeProps`: 상속 `Slider.RangeProps` | - | - | 미구현 | `RangeProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Slider.Thumb` | `SliderThumbProps`: 상속 `Slider.ThumbProps` | - | - | 미구현 | `ThumbProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Slider.HiddenInput` | `SliderHiddenInputProps`: 상속 `Slider.HiddenInputProps` | - | - | 제약 | `HiddenInputProps`: 제약 — RN/Expo UI 레이어에서 DOM hidden input contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 DOM hidden input contract를 제공하지 않는다. |
| `Slider.Tick` | `SliderTickProps`: 상속 `SliderTickVariantProps`, `Slider.TickProps` | - | - | 미구현 | `TickProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Slider.Markers` | `SliderMarkersProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `MarkersProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Slider.Marker` | `SliderMarkerProps`: 상속 `SliderMarkerVariantProps`, `Slider.MarkerProps` | - | - | 미구현 | `MarkerProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Slider.ValueIndicatorRoot` | `SliderValueIndicatorRootProps`: 상속 `Slider.ValueIndicatorRootProps` | - | - | 미구현 | `ValueIndicatorRootProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Slider.ValueIndicatorArrow` | `SliderValueIndicatorArrowProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 미구현 | `ValueIndicatorArrowProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Slider.ValueIndicatorArrowTip` | `SliderValueIndicatorArrowTipProps`: `tipRadius?`; 상속 `svg attrs` | - | - | 미구현 | `ValueIndicatorArrowTipProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Slider.ValueIndicatorLabel` | `SliderValueIndicatorLabelProps`: 상속 `Slider.ValueIndicatorLabelProps` | - | - | 미구현 | `ValueIndicatorLabelProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Snackbar` | `Region`, `RootProvider`, `Root`, `Renderer`, `AvoidOverlap`, `Content`, `Message`, `PrefixIcon`, `ActionButton`, `HiddenCloseButton`; 상속 snackbar primitive/variant props | - | - | 제약 | `ActionButton`: 제약 — Snackbar.ActionButton 행에서 props를 별도 관리한다. / `AvoidOverlap`: 제약 — Snackbar.AvoidOverlap 행에서 props를 별도 관리한다. / `HiddenCloseButton`: 제약 — Snackbar.HiddenCloseButton 행에서 props를 별도 관리한다. / `Message`: 제약 — Snackbar.Message 행에서 props를 별도 관리한다. / `Content`: 제약 — Snackbar.Content 행에서 props를 별도 관리한다. / `PrefixIcon`: 제약 — Snackbar.PrefixIcon 행에서 props를 별도 관리한다. / `Region`: 제약 — Snackbar.Region 행에서 props를 별도 관리한다. / `Renderer`: 제약 — Snackbar.Renderer 행에서 props를 별도 관리한다. / `Root`: 제약 — Snackbar.Root 행에서 props를 별도 관리한다. / `RootProvider`: 제약 — Snackbar.RootProvider 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `Snackbar.ActionButton` | `SnackbarActionButtonProps`: 상속 `PrimitiveProps`, `button attrs` | - | - | 제약 | `ActionButtonProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Snackbar.AvoidOverlap` | `SnackbarAvoidOverlapProps`: 상속 `SnackbarPrimitive.AvoidOverlapProps` | - | - | 제약 | `AvoidOverlapProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Snackbar.HiddenCloseButton` | `SnackbarHiddenCloseButtonProps`: 상속 `SnackbarPrimitive.CloseButtonProps` | - | - | 제약 | `HiddenCloseButtonProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Snackbar.Message` | `SnackbarMessageProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 제약 | `MessageProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Snackbar.Content` | `SnackbarContentProps`: 상속 `PrimitiveProps`, `div attrs` | - | - | 제약 | `ContentProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Snackbar.PrefixIcon` | `SnackbarPrefixIconProps`: 상속 `InternalIconProps` | - | - | 제약 | `PrefixIconProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Snackbar.Region` | `SnackbarRegionProps`: 상속 `SnackbarVariantProps`, `SnackbarPrimitive.RegionProps` | - | - | 제약 | `RegionProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Snackbar.Renderer` | `SnackbarRendererProps`: 상속 `SnackbarPrimitive.RendererProps` | - | - | 제약 | `RendererProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Snackbar.Root` | `SnackbarRootProps`: 상속 `SnackbarVariantProps`, `SnackbarPrimitive.RootProps` | - | - | 제약 | `RootProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Snackbar.RootProvider` | `SnackbarRootProviderProps`: 상속 `SnackbarPrimitive.RootProviderProps` | - | - | 제약 | `RootProviderProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. |
| `Stack` | `StackProps`, `HStackProps`, `VStackProps`: 상속 `Omit<FlexProps, 'flexDirection'>` | `HStack`, `VStack`, `Flex` | `HStack`: `spacing?`, 상속 `Omit<ComponentProps<typeof Row>, 'spacing'>`; `VStack`: `spacing?`, 상속 `Omit<ComponentProps<typeof Column>, 'spacing'>`; `Flex`: `direction?`, `spacing?`, 상속 `Omit<ComponentProps<typeof Row>, 'spacing'>` | 부분완료 | `StackProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `HStackProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `VStackProps`: 부분완료 — Mossy modifier 또는 일부 layout props로만 대응한다. / `FlexProps`: 부분완료 — Mossy modifier 또는 일부 layout props로만 대응한다. / `spacing?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. / `VStack`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. / `Flex`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. / `direction?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 `Stack` 이름 없이 `HStack`/`VStack`을 공개한다. |
| `Switch` | `Root`: `size?`; `Control`, `HiddenInput`, `Label`, `Thumb`; 상속 switch primitive/variant props | `Switch` | `Switch`: `ComponentProps<typeof ExpoSwitch>` | 부분완료 | `Control`: 미구현 — Switch.Control 행에서 props를 별도 관리한다. / `HiddenInput`: 제약 — Switch.HiddenInput 행에서 props를 별도 관리한다. / `Label`: 미구현 — Switch.Label 행에서 props를 별도 관리한다. / `Root`: 부분완료 — Switch.Root 행에서 props를 별도 관리한다. / `Thumb`: 미구현 — Switch.Thumb 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `Switch.Control` | `SwitchControlProps`: 상속 `SwitchmarkVariantProps`, `SwitchPrimitive.ControlProps` | - | - | 미구현 | `ControlProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Switch.HiddenInput` | `SwitchHiddenInputProps`: 상속 `SwitchPrimitive.HiddenInputProps` | - | - | 제약 | `HiddenInputProps`: 제약 — RN/Expo UI 레이어에서 DOM hidden input contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | RN/Expo UI 레이어에서 DOM hidden input contract를 제공하지 않는다. |
| `Switch.Label` | `SwitchLabelProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 미구현 | `LabelProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Switch.Root` | `SwitchRootProps`: `size?`; 상속 `Omit<SwitchVariantProps, "size">`, `Omit<SwitchmarkVariantProps, "size">`, `SwitchPrimitive.RootProps` | `Switch` | `ComponentProps<typeof ExpoSwitch>` | 부분완료 | `RootProps`: 부분완료 — universal `ExpoSwitch` 단일 래퍼로 root 입력 동작만 대응한다. | Seed part props와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | universal `ExpoSwitch` 단일 래퍼로 root 입력 동작만 대응한다. |
| `Switch.Thumb` | `SwitchThumbProps`: 상속 `SwitchPrimitive.ThumbProps` | - | - | 미구현 | `ThumbProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Tabs` | `Root`, `List`, `Trigger`, `Indicator`, `Content`, `Carousel`, `CarouselCamera`; 상속 tabs primitive props, `TabsVariantProps` | - | - | 미구현 | `Content`: 미구현 — Tabs.Content 행에서 props를 별도 관리한다. / `Indicator`: 미구현 — Tabs.Indicator 행에서 props를 별도 관리한다. / `List`: 미구현 — Tabs.List 행에서 props를 별도 관리한다. / `Root`: 미구현 — Tabs.Root 행에서 props를 별도 관리한다. / `CarouselCamera`: 미구현 — Tabs.CarouselCamera 행에서 props를 별도 관리한다. / `Carousel`: 미구현 — Tabs.Carousel 행에서 props를 별도 관리한다. / `Trigger`: 미구현 — Tabs.Trigger 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `Tabs.Content` | `TabsContentProps`: 상속 `TabsPrimitive.ContentProps` | - | - | 미구현 | `ContentProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Tabs.Indicator` | `TabsIndicatorProps`: 상속 `TabsPrimitive.IndicatorProps` | - | - | 미구현 | `IndicatorProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Tabs.List` | `TabsListProps`: 상속 `TabsPrimitive.ListProps` | - | - | 미구현 | `ListProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Tabs.Root` | `TabsRootProps`: 상속 `TabsVariantProps`, `TabsPrimitive.RootProps` | - | - | 미구현 | `RootProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Tabs.CarouselCamera` | `TabsCarouselCameraProps`: 상속 `TabsPrimitive.CarouselCameraProps` | - | - | 미구현 | `CarouselCameraProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Tabs.Carousel` | `TabsCarouselProps`: 상속 `TabsPrimitive.CarouselProps` | - | - | 미구현 | `CarouselProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Tabs.Trigger` | `TabsTriggerProps`: 상속 `TabsPrimitive.TriggerProps` | - | - | 미구현 | `TriggerProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `TagGroup` | `Root`: `separator?`; `Item`, `ItemLabel`; 상속 tag group variant props, `PrimitiveProps`, `StyleProps.flexShrink`, span attrs | - | - | 미구현 | `Root`: 미구현 — TagGroup.Root 행에서 props를 별도 관리한다. / `Item`: 미구현 — TagGroup.Item 행에서 props를 별도 관리한다. / `ItemLabel`: 미구현 — TagGroup.ItemLabel 행에서 props를 별도 관리한다. | 하위 part별 행에서 실제 props parity를 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. |
| `TagGroup.Root` | `TagGroupRootProps`: `separator?`; 상속 `TagGroupVariantProps`, `TagGroupItemVariantProps`, `PrimitiveProps`, `span attrs` | - | - | 미구현 | `RootProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `TagGroup.Item` | `TagGroupItemProps`: 상속 `TagGroupItemVariantProps`, `Pick<StyleProps, "flexShrink">`, `PrimitiveProps`, `span attrs` | - | - | 미구현 | `ItemProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `TagGroup.ItemLabel` | `TagGroupItemLabelProps`: 상속 `PrimitiveProps`, `span attrs` | - | - | 미구현 | `ItemLabelProps`: 미구현 — Mossy 공개 API에 대응 part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy 공개 API에 대응 part가 없다. |
| `Text` | `TextProps`: `as?`, `color?`, `fontSize?`, `lineHeight?`, `fontWeight?`, `maxLines?`, `align?`, `userSelect?`, `whiteSpace?`; 상속 `TextVariantProps`, span attrs | `Text` | `Text`: `textStyle?`, `color?`, `fontSize?`, `lineHeight?`, `fontWeight?`, `align?`, `fontFamily?`, `letterSpacing?`, 상속 `Omit<ComponentProps<typeof ExpoText>, 'textStyle'>` | 부분완료 | `as?`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `color?`: 완료 — Mossy 공개 props에서 동일 이름으로 제공한다. / `fontSize?`: 완료 — Mossy 공개 props에서 동일 이름으로 제공한다. / `lineHeight?`: 완료 — Mossy 공개 props에서 동일 이름으로 제공한다. / `fontWeight?`: 완료 — Mossy 공개 props에서 동일 이름으로 제공한다. / `maxLines?`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `align?`: 완료 — Mossy 공개 props에서 동일 이름으로 제공한다. / `userSelect?`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `whiteSpace?`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `TextVariantProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `span attrs`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `textStyle?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. / `fontFamily?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. / `letterSpacing?`: Mossy only — Mossy 토큰화 또는 Expo UI 조합을 위해 별도 제공한다. | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 `textStyle`, tokenized color/typography props를 제공한다. DOM `as`, `userSelect`, `whiteSpace`는 제공하지 않는다. |
| `TextField` | namespace: `Root`, `Input`, `Textarea`, `PrefixIcon`, `PrefixText`, `SuffixIcon`, `SuffixText` | `TextField` | part별 행 참조 | 부분완료 | `Root`: 부분완료 — Root 행에서 Seed 상태 props와 Mossy 토큰 컨테이너 props 차이를 관리한다. / `Input`: 부분완료 — Input 행에서 Seed input attrs와 Expo UI TextInput props 차이를 관리한다. / `Textarea`: 부분완료 — Textarea 행에서 Seed textarea attrs와 Expo UI multiline props 차이를 관리한다. / `PrefixIcon`: 부분완료 — PrefixIcon 행에서 Seed svg slot과 Mossy Icon props 차이를 관리한다. / `PrefixText`: 부분완료 — PrefixText 행에서 Seed span attrs와 Mossy Text props 차이를 관리한다. / `SuffixIcon`: 부분완료 — SuffixIcon 행에서 Seed svg slot과 Mossy Icon props 차이를 관리한다. / `SuffixText`: 부분완료 — SuffixText 행에서 Seed span attrs와 Mossy Text props 차이를 관리한다. / `seed-design/ui/text-field` snippet: 부분완료 — Mossy v1 snippet은 `snippets/text-field.tsx`에서 관리한다. | 하위 part별 행에서 실제 props parity를 관리하고, Seed snippet 대응은 snippet 문서에서 관리한다. | compound namespace 자체는 props를 갖지 않는 공개 API 묶음이다. Seed snippet과 같은 DX wrapper는 패키지 export가 아니라 복사용 snippet이다. |
| `TextField.Root` | `TextFieldRootProps`: 상속 `TextInputVariantProps`, `TextField.RootProps`; div attrs | `TextField.Root`, `TextFieldRoot` | `children?`, `value?`, `defaultValue?`, `onValueChange?`, `disabled?`, `invalid?`, `readOnly?`, `textStyle?`, `color?`, `placeholderColor?`, `backgroundColor?`, `borderColor?`, `height?`, `paddingHorizontal?`, `paddingVertical?`, `radius?`, `borderWidth?`, `spacing?`, `alignment?`, `style?`, `testID?`, `modifiers?` | 부분완료 | `value?`: 완료 — Mossy Root context에서 Input/Textarea로 주입한다. / `defaultValue?`: 완료 — Mossy Root context에서 Input/Textarea로 주입한다. / `onValueChange?`: 완료 — Mossy Root context에서 `onChangeText`로 주입한다. / `disabled?`: 완료 — Mossy Root context와 HStack disabled로 주입한다. / `invalid?`: 완료 — Mossy Root context와 기본 borderColor 결정에 사용한다. / `readOnly?`: 완료 — Mossy Root context에서 Input/Textarea로 주입한다. / `TextInputVariantProps`: 부분완료 — Seed CSS recipe variant를 Mossy 토큰 props(`textStyle`, `color`, `backgroundColor`, `borderColor`, spacing 계열)로 재해석한다. / `div attrs`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `children?`: Mossy only — compound children 배치를 위해 제공한다. / `textStyle?`: Mossy only — Mossy typography token을 Root context로 주입한다. / `color?`: Mossy only — 입력·affix 기본 색상 토큰을 Root context로 주입한다. / `placeholderColor?`: Mossy only — placeholder 색상 토큰을 Root context로 주입한다. / `backgroundColor?`: Mossy only — HStack 배경 토큰을 제공한다. / `borderColor?`: Mossy only — HStack border 토큰을 제공한다. / `height?`: Mossy only — HStack 높이 토큰/값을 제공한다. / `paddingHorizontal?`: Mossy only — HStack 가로 padding 토큰/값을 제공한다. / `paddingVertical?`: Mossy only — HStack 세로 padding 토큰/값을 제공한다. / `radius?`: Mossy only — HStack radius 토큰/값을 제공한다. / `borderWidth?`: Mossy only — HStack border width 토큰/값을 제공한다. / `spacing?`: Mossy only — affix와 input 간격 토큰/값을 제공한다. / `alignment?`: Mossy only — HStack 정렬을 제공한다. / `style?`: Mossy only — HStack style escape hatch다. / `testID?`: Mossy only — RN 테스트 식별자다. / `modifiers?`: Mossy only — Expo UI modifier escape hatch다. | Seed CSS variant와 DOM attrs를 그대로 복제하지 않고 Mossy token props를 기준으로 유지한다. | Seed Root는 CSS recipe provider와 web TextField state provider다. Mossy Root는 HStack 컨테이너와 RN input state context다. |
| `TextField.Input` | `TextFieldInputProps`: 상속 `TextField.InputProps`; input attrs; `className?`; `ref` | `TextField.Input`, `TextFieldInput` | `MossyTextFieldInputProps`: 상속 `Omit<ComponentProps<typeof TextInput>, 'placeholderTextColor', 'style', 'textStyle'>`; `textStyle?`, `color?`, `placeholderColor?`, `style?` | 부분완료 | `TextField.InputProps`: 부분완료 — value/defaultValue/onValueChange/disabled/readOnly는 Mossy Root context에서 `TextInput`으로 주입한다. / `input attrs`: 제약 — RN/Expo UI 레이어에서 동일 DOM input attrs contract를 제공하지 않는다. / `className?`: 제약 — Expo UI TextInput modifier/style contract로 대체한다. / `ref`: 제약 — React 19 일반 ref prop과 Expo UI component contract를 따른다. / `TextInput props`: 부분완료 — Expo UI universal `TextInput` props를 기반으로 전달한다. / `textStyle?`: Mossy only — Mossy typography token을 `textStyle`로 해석한다. / `color?`: Mossy only — Mossy color token 또는 원시 색상을 입력 텍스트 색으로 해석한다. / `placeholderColor?`: Mossy only — Mossy color token 또는 원시 색상을 `placeholderTextColor`로 해석한다. / `style?`: Mossy only — 기본 transparent input style 뒤에 병합한다. | Seed input attrs 전체 parity 대신 Expo UI TextInput props와 토큰 props를 기준으로 관리한다. | Root context가 있으면 value/defaultValue/onValueChange/disabled/readOnly/text token을 주입한다. Seed의 Field context ARIA/name 병합은 RN에서 동일 contract가 없다. |
| `TextField.Textarea` | `TextFieldTextareaProps`: 상속 `TextField.TextareaProps`; textarea attrs; `autoresize?`; `className?`; `ref` | `TextField.Textarea`, `TextFieldTextarea` | `MossyTextFieldTextareaProps`: `MossyTextFieldInputProps`에서 `multiline` 제외; `autoresize?` | 부분완료 | `TextField.TextareaProps`: 부분완료 — value/defaultValue/onValueChange/disabled/readOnly는 Mossy Root context에서 multiline `TextInput`으로 주입한다. / `textarea attrs`: 제약 — RN/Expo UI 레이어에서 동일 DOM textarea attrs contract를 제공하지 않는다. / `autoresize?`: 부분완료 — Seed는 DOM scrollHeight를 측정해 height를 갱신하고, Mossy는 native multiline 자연 크기 또는 고정 줄 수 예약으로 대응한다. / `className?`: 제약 — Expo UI TextInput modifier/style contract로 대체한다. / `ref`: 제약 — React 19 일반 ref prop과 Expo UI component contract를 따른다. / `MossyTextFieldInputProps`: 부분완료 — Input 행의 Expo UI TextInput props와 token props를 공유한다. | RN multiline 동작 기준으로 autoresize 의미를 유지하고, DOM textarea props는 복제하지 않는다. | `autoresize=false`일 때 `numberOfLines` 또는 `rows` 값으로 고정 줄 수를 예약한다. |
| `TextField.PrefixIcon` | `TextFieldPrefixIconProps`: 상속 `InternalIconProps`; svg slot attrs | `TextField.PrefixIcon`, `TextFieldPrefixIcon` | `MossyIconProps` | 부분완료 | `InternalIconProps.svg`: 제약 — Seed는 SVG ReactNode slot을 받고, Mossy는 Expo UI `Icon` props/source contract를 따른다. / `svg slot attrs`: 제약 — RN/Expo UI 레이어에서 동일 SVG DOM attrs contract를 제공하지 않는다. / `MossyIconProps`: 부분완료 — `size?`, `color?`를 Mossy 토큰/값으로 제공하고 나머지는 Expo UI Icon props를 따른다. | Seed svg slot parity 대신 Expo UI Icon 기반 토큰 API를 유지한다. | Root context가 있으면 기본 size `x5`, disabled/invalid 상태 기반 기본 color를 주입한다. |
| `TextField.PrefixText` | `TextFieldPrefixTextProps`: 상속 `PrimitiveProps`, span attrs | `TextField.PrefixText`, `TextFieldPrefixText` | `MossyTextProps` | 부분완료 | `PrimitiveProps`: 제약 — RN/Expo UI 레이어에서 동일 web primitive contract를 제공하지 않는다. / `span attrs`: 제약 — RN/Expo UI 레이어에서 동일 DOM span attrs contract를 제공하지 않는다. / `MossyTextProps`: 부분완료 — Text 행의 tokenized typography/color props와 Expo UI Text props를 따른다. | Seed span attrs parity 대신 Mossy Text 기반 토큰 API를 유지한다. | Root context가 있으면 textStyle과 disabled/invalid 상태 기반 기본 color를 주입한다. |
| `TextField.SuffixIcon` | `TextFieldSuffixIconProps`: 상속 `InternalIconProps`; svg slot attrs | `TextField.SuffixIcon`, `TextFieldSuffixIcon` | `MossyIconProps` | 부분완료 | `InternalIconProps.svg`: 제약 — Seed는 SVG ReactNode slot을 받고, Mossy는 Expo UI `Icon` props/source contract를 따른다. / `svg slot attrs`: 제약 — RN/Expo UI 레이어에서 동일 SVG DOM attrs contract를 제공하지 않는다. / `MossyIconProps`: 부분완료 — `size?`, `color?`를 Mossy 토큰/값으로 제공하고 나머지는 Expo UI Icon props를 따른다. | Seed svg slot parity 대신 Expo UI Icon 기반 토큰 API를 유지한다. | Root context가 있으면 기본 size `x5`, disabled/invalid 상태 기반 기본 color를 주입한다. |
| `TextField.SuffixText` | `TextFieldSuffixTextProps`: 상속 `PrimitiveProps`, span attrs | `TextField.SuffixText`, `TextFieldSuffixText` | `MossyTextProps` | 부분완료 | `PrimitiveProps`: 제약 — RN/Expo UI 레이어에서 동일 web primitive contract를 제공하지 않는다. / `span attrs`: 제약 — RN/Expo UI 레이어에서 동일 DOM span attrs contract를 제공하지 않는다. / `MossyTextProps`: 부분완료 — Text 행의 tokenized typography/color props와 Expo UI Text props를 따른다. | Seed span attrs parity 대신 Mossy Text 기반 토큰 API를 유지한다. | Root context가 있으면 textStyle과 disabled/invalid 상태 기반 기본 color를 주입한다. |
| `ToggleButton` | `ToggleButtonProps`: 상속 `ToggleButtonVariantProps`, `UsePendingButtonProps`, toggle primitive props | - | - | 미구현 | `ToggleButtonVariantProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `UsePendingButtonProps`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. / `toggle primitive props`: 미구현 — Mossy 공개 API에 대응 prop/part가 없다. | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Toggle/pressed 상태 토큰 정책 확인 필요. |
| `VisuallyHidden` | `VisuallyHiddenProps`: 상속 `PrimitiveProps`, div attrs | - | - | 제약 | `PrimitiveProps`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. / `div attrs`: 제약 — RN/Expo UI 레이어에서 동일 web contract를 제공하지 않는다. | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | Web accessibility utility다. RN 접근성 props로 별도 처리한다. |

## Mossy Only

| Mossy 컴포넌트 | Mossy props | 이유 |
| --- | --- | --- |
| `ScrollView` | `style?`, `padding?`, `paddingHorizontal?`, `paddingVertical?`, `paddingTop?`, `paddingBottom?`, `paddingLeft?`, `paddingRight?`, 상속 `Omit<ExpoScrollViewProps, 'style'>` | Seed React에는 직접 대응 컴포넌트가 없다. Mossy는 RN ScrollView padding 토큰화를 위해 제공한다. |
| `Spacer` | `size?`, 상속 `Omit<ComponentProps<typeof ExpoSpacer>, 'size'>` | Seed React에는 직접 대응 컴포넌트가 없다. Mossy는 Expo UI Spacer 크기를 토큰화해 제공한다. |
