# Seed Component Parity

Seed Design 구현 진행 상태를 Seed React 컴포넌트 기준으로 점검하기 위한 체크리스트.

## 기준

- **Seed 기준 경로** — `.ai/refs/seed-design/packages/react/src/components`.
- **mossy 기준 경로** — `src/index.ts`가 named export하는 `src/components/*`.
- **체크리스트 기준** — Seed React 컴포넌트 1개를 1행으로 기록하고, Mossy 대응·현재 props·상태·다음 액션을 같은 행에서 관리한다.
- **Props 표기** — Seed는 로컬 참조본의 `*Props` 타입 기준으로 명시 props와 상속 props 타입명을 기록한다.
- **상태 점검** — 컴포넌트 추가·수정 전 `Seed Implementation Checklist`의 상태를 확인하고, 작업 후 Mossy 대응·Mossy props·상태·다음 액션·메모를 갱신한다.
- **Mossy 전용 API** — Seed Design 구현 목표에 포함되지 않는 Mossy 전용 컴포넌트는 `Mossy Only` 섹션에서 별도 관리한다.

## 상태

| 상태 | 의미 |
| --- | --- |
| 완료 | Seed 대응 범위를 Mossy RN/Expo UI API로 구현 완료했다. |
| 부분완료 | Mossy 대응 컴포넌트는 있으나 Seed props·파트·variant 대비 누락 범위가 남아 있다. |
| 미구현 | Seed 컴포넌트를 확인했지만 mossy-ui 공개 API에 대응 컴포넌트가 없다. |
| 제약 | DOM/HTML, Ark web primitive, web portal, web-only CSS layout, Expo UI native slot 제약 때문에 Seed API 형태를 그대로 제공하지 않는다. |
| Mossy only | seed-design에는 없으나 mossy-ui에서만 제공한다. |

## Seed Implementation Checklist

| Seed 컴포넌트 | Seed props 요약 | Mossy 대응 | Mossy props | 상태 | 다음 액션 | 메모 |
| --- | --- | --- | --- | --- | --- | --- |
| `ActionButton` | `ActionButtonProps`: `color?`, `fontWeight?`; 상속 `ActionButtonVariantProps`, `UsePendingButtonProps`, `PrimitiveProps`, `StyleProps.flexGrow/bleedX/bleedY`, button attrs | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 버튼 계열 토큰 API 설계 필요. |
| `ActionChip` | `ActionChipProps`: 상속 `ActionChipVariantProps`, `PrimitiveProps`, button attrs | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Chip 계열 미구현. |
| `ActionSheet` | `Root`: `lazyMount?`, `unmountOnExit?`; `Backdrop`, `CloseButton`, `Content`, `Description`, `Header`, `Item`, `List`, `Positioner`, `Title`, `Trigger` 상속 Dialog/HTML props | - | - | 제약 | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | Seed는 Ark Dialog 기반 compound web API다. Expo UI 네이티브 sheet와 1:1 파트 매핑이 없다. |
| `Article` | `ArticleProps`: 상속 `BoxProps` | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 웹 semantic wrapper. RN에서는 별도 의미가 약하다. |
| `AspectRatio` | `AspectRatioProps`: `ratio?`, `position?`, `overflowX?`, `overflowY?`; 상속 `BoxProps` | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | RN `aspectRatio` 기반 구현 가능 여부 확인 필요. |
| `Avatar` | `Root`, `Image`, `Fallback`, `Badge`, `Stack`; 상속 `AvatarVariantProps`, `AvatarStackVariantProps`, Ark image props, `PrimitiveProps`, HTML attrs | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Image/Fallback/Badge 조합 API 설계 필요. |
| `Badge` | `BadgeProps`: 상속 `BadgeVariantProps`, `PrimitiveProps`, span attrs | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 토큰 variant 래퍼 설계 필요. |
| `BottomSheet` | `Root`, `Trigger`, `Positioner`, `Backdrop`, `Content`, `Header`, `Title`, `Description`, `Body`, `Footer`, `CloseButton`; `Body`는 `paddingX`, `height`, `maxHeight`, `minHeight`, `justifyContent`, `alignItems` 상속 | - | - | 제약 | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | Seed는 Ark Drawer 기반 compound web API다. Expo UI BottomSheet는 플랫폼별 네이티브 API 제약이 있다. |
| `BottomSheetHandle` | `BottomSheetHandleProps`: 상속 `PrimitiveProps`, div attrs | - | - | 제약 | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | BottomSheet 내부 파트로만 의미가 있다. |
| `Box` | `BoxProps`: `as?`, `asChild?`; 상속 `StyleProps`, div attrs | `Box` | `Box`: `children?`, `alignment?`, `testID?`, `modifiers?` | 부분완료 | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 Expo UI modifier 중심으로 `alignment`, `modifiers`만 공개한다. DOM `as`, HTML attrs는 제공하지 않는다. |
| `Callout` | `Root`, `Content`, `Title`, `Description`, `Link`, `CloseButton`; 상속 `CalloutVariantProps`, `DismissibleRootProps`, `PrimitiveProps`, HTML/button attrs | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 배너/알림 계열 토큰 API 설계 필요. |
| `Celsius` | `CelsiusProps`: `value` | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 도메인 특화 표시 컴포넌트. |
| `Checkbox` | `Root`: `weight?`; `Indicator`: `unchecked?`, `checked`, `indeterminate?`; `Group`, `Control`, `HiddenInput`, `Label`; 상속 `CheckboxVariantProps`, `CheckmarkVariantProps`, Ark checkbox props | `Checkbox` | `Checkbox`: `ComponentProps<typeof ExpoCheckbox>` | 부분완료 | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 universal `ExpoCheckbox` pass-through다. Seed의 파트 분해 API는 제공하지 않는다. |
| `Chip` | `Root`, `Label`, `PrefixIcon`, `PrefixAvatar`, `SuffixIcon`; 상속 `ChipVariantProps`, `PrimitiveProps`, button/span/div attrs | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | compound 파트 대신 닫힌 props/children 구조 필요. |
| `ChipTabs` | `Root`, `List`, `Trigger`, `Content`, `Carousel`, `CarouselCamera`; 상속 `ChipTabsVariantProps`, tabs primitive props | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | SegmentedControl과 별개로 탭 상태/콘텐츠 API 필요. |
| `Columns` | `ColumnsProps`: 상속 `BoxProps`; `ColumnProps`: `width?` | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | RN flex/grid 대응 정책 확인 필요. |
| `ConsistentWidth` | `ConsistentWidthProps`: `children`; 상속 `PrimitiveProps`, span attrs | - | - | 제약 | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | 웹 텍스트 측정/hidden span 기반 유틸 성격이다. |
| `ContentPlaceholder` | `Root`: 상속 `ContentPlaceholderVariantProps`, `PrimitiveProps`, div attrs; `Asset`: HTML attrs | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Placeholder/Skeleton 계열 설계 필요. |
| `ContextualFloatingButton` | `ContextualFloatingButtonProps`: 상속 `ContextualFloatingButtonVariantProps`, `UsePendingButtonProps`, `PrimitiveProps`, button attrs | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | FAB 계열과 함께 검토. |
| `ControlChip` | `ControlChipProps`, `ControlChipBaseProps`: 상속 `ControlChipVariantProps`, `PrimitiveProps`, button attrs | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 선택형 chip API 필요. |
| `Count` | `CountProps`: 상속 span attrs | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 숫자 표시 토큰 컴포넌트. |
| `Dialog` | `Root`: `lazyMount?`, `unmountOnExit?`; `Trigger`, `Positioner`, `Backdrop`, `Content`, `Header`, `Title`, `Description`, `Footer`, `Action`; 상속 Dialog primitive/HTML props | - | - | 제약 | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | Seed는 web dialog primitive다. RN/Expo UI에서 동일한 DOM focus/portal contract를 제공하지 않는다. |
| `Divider` | `DividerProps`: `as?`, `color?`, `thickness?`, `orientation?`, `inset?`; 상속 hr attrs | `Divider` | `Divider`: `orientation?`, `thickness?`, `color?`, `inset?`, `testID?`, `modifiers?` | 부분완료 | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 `orientation`, `thickness`, `color`, `inset` 토큰/값 중심이다. DOM `as`는 제공하지 않는다. |
| `ExtendedActionSheet` | `Root`: `lazyMount?`, `unmountOnExit?`; `Backdrop`, `CloseButton`, `Content`, `Footer`, `Group`, `Header`, `Item`, `List`, `Positioner`, `Title`, `Trigger`; 상속 Dialog/HTML props | - | - | 제약 | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | Seed web compound action sheet API다. |
| `ExtendedFab` | `ExtendedFabProps`: 상속 `ExtendedFabVariantProps`, `PrimitiveProps`, button attrs | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Expo UI platform FAB 가능성 확인 필요. |
| `Fab` | `FabProps`: 상속 `FabVariantProps`, `PrimitiveProps`, button attrs | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Expo UI platform FAB 가능성 확인 필요. |
| `Field` | `Root`, `Header`, `Label`, `IndicatorText`, `RequiredIndicator`, `Description`, `ErrorMessage`, `Footer`, `CharacterCount`; `CharacterCount`: `current`, `max`; 상속 field primitive/label variant props | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 입력 필드 공통 래퍼 설계 필요. |
| `FieldButton` | `Root`, `Header`, `Label`, `IndicatorText`, `RequiredIndicator`, `PrefixIcon`, `PrefixText`, `SuffixIcon`, `SuffixText`, `Footer`, `Description`, `ErrorMessage`, `HiddenInput`, `Button`, `Control`, `ClearButton`, `Value`, `Placeholder`; 상속 field button primitive props | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | TextField/Picker 계열과 함께 검토. |
| `Fieldset` | `Root`, `Header`, `Label`, `IndicatorText`, `RequiredIndicator`, `Description`, `ErrorMessage`, `Footer`; 상속 fieldset primitive/label variant props | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 그룹 입력 API 필요. |
| `Flex` | `FlexProps`: `display?`, `direction?`, `wrap?`, `align?`, `justify?`, `grow?`, `shrink?`; 상속 `BoxProps` | `Flex`, `HStack`, `VStack` | `Flex`: `direction?`, `spacing?`, 상속 `Omit<ComponentProps<typeof Row>, 'spacing'>`; `HStack`: `spacing?`, 상속 `Omit<ComponentProps<typeof Row>, 'spacing'>`; `VStack`: `spacing?`, 상속 `Omit<ComponentProps<typeof Column>, 'spacing'>` | 부분완료 | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 Expo UI `Row`/`Column` 기반 `direction`, `spacing` 중심이다. |
| `Float` | `FloatProps`: `as?`, `placement`, `offsetX?`, `offsetY?`, `zIndex?`; 상속 div attrs | `Float` | `Float`: `children?`, `placement`, `offsetX?`, `offsetY?`, `testID?`, `modifiers?` | 부분완료 | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 `placement`, `offsetX`, `offsetY`, `modifiers` 중심이다. DOM `as`는 제공하지 않는다. |
| `FloatingActionButton` | `Root`, `Icon`, `Label`; 상속 `FloatingActionButtonVariantProps`, `PrimitiveProps`, button/span attrs, `InternalIconProps` | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Expo UI platform FAB 가능성 확인 필요. |
| `Grid` | `GridProps`: `display?`, `align?`, `justify?`, `justifyItems?`, `columns?`, `rows?`, `autoFlow?`, `autoColumns?`, `autoRows?`; 상속 `BoxProps` | `Grid` | `Grid`: `children?`, `columns`, `horizontalSpacing?`, `verticalSpacing?`, `testID?`, `modifiers?` | 부분완료 | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 `columns`, `horizontalSpacing`, `verticalSpacing` 중심이다. CSS grid 세부 props는 제공하지 않는다. |
| `GridItem` | `GridItemProps`: `asChild?`, `colSpan?`, `rowSpan?`, `colStart?`, `colEnd?`, `rowStart?`, `rowEnd?`; 상속 `BoxProps` | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Mossy Grid item span API 추가 여부 확인 필요. |
| `HelpBubble` | `Root`: `placement?`, `gutter?`, `overflowPadding?`, `arrowPadding?`; `Anchor`, `Arrow`, `ArrowTip`, `Body`, `CloseButton`, `Content`, `Description`, `PositionerPortal`, `Positioner`, `Title`, `Trigger`; 상속 Popover primitive/HTML props | - | - | 제약 | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | Web popover/portal/positioner contract가 RN/Expo UI와 다르다. |
| `Icon` | `IconProps`: `svg`, `size?`, `color?`; `PrefixIconProps`: `svg`; `SuffixIconProps`: `svg` | `Icon` | `Icon`: `color?`, `size?`, 상속 `Omit<ComponentProps<typeof ExpoIcon>, 'color' | 'size'>` | 부분완료 | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 Expo UI Icon pass-through에 `color`, `size` 토큰 prop을 주입한다. |
| `IdentityPlaceholder` | `Root`: 상속 `IdentityPlaceholderVariantProps`, `PrimitiveProps`, div attrs; `Image`: SVG props | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Avatar/Placeholder 계열과 함께 검토. |
| `ImageFrame` | `ImageFrameProps`: `rounded?`, `src`, `alt`, `fallback?`, `loading?`, `decoding?`, `crossOrigin?`, `referrerPolicy?`, `sizes?`, `srcSet?`, `onLoad?`, `onError?`, `children?`; `Floater`, `Badge`, `Icon`, `Indicator`, `ReactionButton` | - | - | 제약 | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | Web image attributes와 responsive image props가 RN Image API와 다르다. |
| `Inline` | `InlineProps`: 상속 `BoxProps` | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | HStack/Flex로 대응 가능하나 별도 공개 API 없음. |
| `InlineBanner` | `Root`, `Content`, `Title`, `Description`, `Link`, `CloseButton`; 상속 `InlineBannerVariantProps`, `DismissibleRootProps`, `PrimitiveProps`, HTML/button attrs | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Banner/Callout 계열 설계 필요. |
| `LinkContent` | `LinkContentProps`: 상속 `LinkContentVariantProps`, `PrimitiveProps`, `StyleProps.color`, span attrs | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Link/pressable 텍스트 API 필요. |
| `List` | `Root`: `itemBorderRadius?`; `Header`: `as?`; `Item`, `Content`, `Title`, `Detail`, `Prefix`, `Suffix`; 상속 `VStackProps`, `ListHeaderVariantProps`, `ListItemVariantProps`, `StyleProps`, HTML attrs | `List`, `ListHeader`, `ListItem` | `List`: `ComponentProps<typeof ExpoList>`; `ListHeader`: `title`, `variant?`, `children?`, `testID?`, `modifiers?`; `ListItem`: `title?`, `detail?`, 상속 `ComponentProps<typeof ExpoListItem>` | 부분완료 | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 Expo UI `List`/`ListItem`과 별도 `ListHeader`를 제공한다. Seed의 compound slots는 그대로 재노출하지 않는다. |
| `LoadingIndicator` | `LoadingIndicatorProps`: `indicator`; 상속 span attrs | `LoadingIndicator` | `LoadingIndicator`: 상속 `Omit<MossyProgressCircleProps, 'value'>` | 부분완료 | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 `ProgressCircle` 기반 indeterminate 래퍼다. |
| `MannerTemp` | `MannerTempProps`: 상속 `MannerTempVariantProps`, `PrimitiveProps`, span attrs; `MannerTempEmoteProps`: `level?` | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 도메인 특화 표시 컴포넌트. |
| `MannerTempBadge` | `MannerTempBadgeProps`: 상속 `MannerTempBadgeVariantProps`, `PrimitiveProps`, span attrs | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 도메인 특화 표시 컴포넌트. |
| `MenuSheet` | `Root`: `lazyMount?`, `unmountOnExit?`; `Backdrop`, `CloseButton`, `Content`, `Description`, `Footer`, `Group`, `Header`, `Item`, `ItemContent`, `ItemDescription`, `ItemLabel`, `List`, `Positioner`, `Title`, `Trigger`; 상속 Dialog primitive/HTML props | - | - | 제약 | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | Seed web dialog/menu sheet compound API다. |
| `NotificationBadge` | `NotificationBadgeProps`, `NotificationBadgePositionerProps`: 상속 notification badge variant props, `PrimitiveProps`, span attrs | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Badge 계열과 함께 검토. |
| `PageBanner` | `Root`, `Content`, `Body`, `Title`, `Description`, `Button`, `CloseButton`; 상속 `PageBannerVariantProps`, `DismissibleRootProps`, `PrimitiveProps`, HTML/button attrs | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Banner/Callout 계열 설계 필요. |
| `Portal` | `PortalProps`: `@seed-design/react-portal`에서 재노출 | - | - | 제약 | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | Web portal API다. Expo UI Host/네이티브 modal 계층과 동일하게 제공하지 않는다. |
| `ProgressCircle` | `Root`, `Track`, `Range`; 상속 `ProgressCirclePrimitive.*Props`, `ProgressCircleVariantProps` | `ProgressCircle` | `ProgressCircle`: `value?`, `color?`, `trackColor?`, `testID?`, `modifiers?` | 부분완료 | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 `value`, `color`, `trackColor`, `modifiers` 중심 단일 컴포넌트다. |
| `PullToRefresh` | `Root`, `Content`, `Indicator`; 상속 pull-to-refresh primitive props, `PullToRefreshVariantProps` | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | RN RefreshControl/ScrollView 조합 검토 필요. |
| `RadioGroup` | `Root`, `Item`, `ItemControl`, `ItemHiddenInput`, `ItemIndicator`, `ItemLabel`; `ItemIndicator`: `unchecked?`, `checked?`; 상속 radio group primitive/variant props | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Expo UI 대응 컴포넌트 확인 필요. |
| `RadioGroupField` | `Root`, `Header`, `Label`, `IndicatorText`, `RequiredIndicator`, `Description`, `ErrorMessage`, `Footer`; 상속 field/radio primitive props | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | RadioGroup과 Field 조합 설계 필요. |
| `ReactionButton` | `ReactionButtonProps`: 상속 `ReactionButtonVariantProps`, `UsePendingButtonProps`, toggle primitive props | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | ToggleButton/FAB 계열과 함께 검토. |
| `ResponsivePair` | `ResponsivePairProps`: `wrap?`, `children`; 상속 `FlexProps` | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | RN 반응형 기준과 breakpoint 정책 필요. |
| `ScrollFog` | `ScrollFogProps`: `placement?`, `size?`, `sizes?`; 상속 `ScrollFogVariantProps`, div attrs | `ScrollFog` | `ScrollFog`: `children?`, `placement?`, `size?`, `sizes?`, 상속 `ViewProps` | 부분완료 | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 RN `ViewProps`와 토큰 size를 사용한다. |
| `SegmentedControl` | `Root`, `Item`, `ItemHiddenInput`, `Indicator`; 상속 segmented control primitive/variant props | `SegmentedControl` | `SegmentedControl`: `options`, `selectedIndex`, `onSelectedIndexChange`, `disabled?`, `testID?`, `modifiers?` | 부분완료 | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 `options`, `selectedIndex`, `onSelectedIndexChange`의 닫힌 데이터 props를 사용한다. |
| `SelectBox` | `CheckSelectBox*`, `RadioSelectBox*`; `Group`: `columns?`; `Root`: `footerVisibility?`; 상속 checkbox/radio primitive props, select box variant props, `InternalIconProps` | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | RN 선택 카드 API로 재설계 필요. |
| `Skeleton` | `SkeletonProps`: 상속 `SkeletonVariantProps`, `PrimitiveProps`, `StyleProps.height/width`, div attrs | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Loading 문서 기준으로 구현 가능성 확인 필요. |
| `Slider` | `Root`, `Control`, `Track`, `Range`, `Thumb`, `HiddenInput`, `Markers`, `Marker`, `Tick`, `ValueIndicatorRoot`, `ValueIndicatorLabel`, `ValueIndicatorArrow`, `ValueIndicatorArrowTip`; `ArrowTip`: `tipRadius?`; 상속 slider primitive/variant props | `Slider` | `Slider`: `ComponentProps<typeof ExpoSlider>` | 부분완료 | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 universal `ExpoSlider` pass-through다. Seed의 marker/value indicator 파트는 제공하지 않는다. |
| `Snackbar` | `Region`, `RootProvider`, `Root`, `Renderer`, `AvoidOverlap`, `Content`, `Message`, `PrefixIcon`, `ActionButton`, `HiddenCloseButton`; 상속 snackbar primitive/variant props | - | - | 제약 | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | Web toast/snackbar portal region contract가 RN/Expo UI와 다르다. |
| `Stack` | `StackProps`, `HStackProps`, `VStackProps`: 상속 `Omit<FlexProps, 'flexDirection'>` | `HStack`, `VStack`, `Flex` | `HStack`: `spacing?`, 상속 `Omit<ComponentProps<typeof Row>, 'spacing'>`; `VStack`: `spacing?`, 상속 `Omit<ComponentProps<typeof Column>, 'spacing'>`; `Flex`: `direction?`, `spacing?`, 상속 `Omit<ComponentProps<typeof Row>, 'spacing'>` | 부분완료 | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 `Stack` 이름 없이 `HStack`/`VStack`을 공개한다. |
| `Switch` | `Root`: `size?`; `Control`, `HiddenInput`, `Label`, `Thumb`; 상속 switch primitive/variant props | `Switch` | `Switch`: `ComponentProps<typeof ExpoSwitch>` | 부분완료 | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 universal `ExpoSwitch` pass-through다. Seed의 파트 분해 API는 제공하지 않는다. |
| `Tabs` | `Root`, `List`, `Trigger`, `Indicator`, `Content`, `Carousel`, `CarouselCamera`; 상속 tabs primitive props, `TabsVariantProps` | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | SegmentedControl과 다른 콘텐츠 탭 API 필요. |
| `TagGroup` | `Root`: `separator?`; `Item`, `ItemLabel`; 상속 tag group variant props, `PrimitiveProps`, `StyleProps.flexShrink`, span attrs | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | 텍스트 태그 그룹 API 필요. |
| `Text` | `TextProps`: `as?`, `color?`, `fontSize?`, `lineHeight?`, `fontWeight?`, `maxLines?`, `align?`, `userSelect?`, `whiteSpace?`; 상속 `TextVariantProps`, span attrs | `Text` | `Text`: `textStyle?`, `color?`, `fontSize?`, `lineHeight?`, `fontWeight?`, `align?`, `fontFamily?`, `letterSpacing?`, 상속 `Omit<ComponentProps<typeof ExpoText>, 'textStyle'>` | 부분완료 | Seed props/parts와 Mossy API 차이를 확인하고 필요한 토큰 prop·닫힌 데이터 prop을 보강한다. | Mossy는 `textStyle`, tokenized color/typography props를 제공한다. DOM `as`, `userSelect`, `whiteSpace`는 제공하지 않는다. |
| `TextField` | `Root`, `Input`, `Textarea`, `PrefixIcon`, `PrefixText`, `SuffixIcon`, `SuffixText`; `Textarea`: `autoresize?`; 상속 text field primitive/variant props | `TextInput` | `TextInput`: `textStyle?`, `color?`, `placeholderColor?`, `backgroundColor?`, `borderColor?`, `height?`, `paddingHorizontal?`, `radius?`, `style?`, 상속 `Omit<ComponentProps<typeof ExpoTextInput>, 'placeholderTextColor' \| 'style' \| 'textStyle'>` | 부분완료 | Field 래퍼·prefix/suffix 슬롯·textarea 정책을 별도 설계한다. | Mossy는 universal `ExpoTextInput` 기반 단일 입력 래퍼를 제공한다. Seed TextField의 compound parts는 아직 제공하지 않는다. |
| `ToggleButton` | `ToggleButtonProps`: 상속 `ToggleButtonVariantProps`, `UsePendingButtonProps`, toggle primitive props | - | - | 미구현 | 구현 필요성과 `@expo/ui` 대응 가능성을 확인한 뒤 Mossy API를 설계한다. | Toggle/pressed 상태 토큰 정책 확인 필요. |
| `VisuallyHidden` | `VisuallyHiddenProps`: 상속 `PrimitiveProps`, div attrs | - | - | 제약 | Seed API를 그대로 구현하지 않고 RN/Expo UI 대체 패턴을 문서화한다. | Web accessibility utility다. RN 접근성 props로 별도 처리한다. |

## Mossy Only

| Mossy 컴포넌트 | Mossy props | 이유 |
| --- | --- | --- |
| `ScrollView` | `style?`, `padding?`, `paddingHorizontal?`, `paddingVertical?`, `paddingTop?`, `paddingBottom?`, `paddingLeft?`, `paddingRight?`, 상속 `Omit<ExpoScrollViewProps, 'style'>` | Seed React에는 직접 대응 컴포넌트가 없다. Mossy는 RN ScrollView padding 토큰화를 위해 제공한다. |
| `Spacer` | `size?`, 상속 `Omit<ComponentProps<typeof ExpoSpacer>, 'size'>` | Seed React에는 직접 대응 컴포넌트가 없다. Mossy는 Expo UI Spacer 크기를 토큰화해 제공한다. |
